[
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-05 12:16:12.813461",
  "module": null,
  "name": "Auto Set the Custom Puchase Reciept",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Receipt",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Receipt\n# DocType: Purchase Receipt  |  Event: before_save\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    price        = frappe.utils.flt(row.rate)\n    qty          = frappe.utils.flt(row.qty)\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').lower()\n\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    inventory_qty = qty + scheme_qty\n\n    gross_total       = price * effective_qty\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n    fed_amount        = price * effective_qty * (fed_per / 100.0)\n\n    if 'exempted' in formula or 'gst exempt' in formula:\n        gst_amount = 0.0\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        gst_amount = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'trade offer' in formula:\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        gst_amount = price * qty * (gst_rate / 100.0)\n    else:\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    if adv_tax_per == 0.0:\n        adv_tax_amount = 0.0\n    elif 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n    elif 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n    elif 'gst' in formula and 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n    elif 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n    else:\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    line_total     = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(valuation_rate, 4)\n    row.amount                    = frappe.utils.flt(line_total, 2)\n\n    row.qty            = frappe.utils.flt(inventory_qty, 3)\n    row.valuation_rate = frappe.utils.flt(valuation_rate, 4)\n\n    total_qty     += inventory_qty\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception:\n    pass\n\ndoc.total_qty = total_qty",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-10 11:31:33.064380",
  "module": null,
  "name": "Auto Set Inventory and rate in reciept",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Receipt",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Receipt\n# DocType : Purchase Receipt\n# Event   : Before Save\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    # ── Read all inputs ──────────────────────────────────────\n    price        = frappe.utils.flt(row.rate)\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').lower()\n\n    # FIX: qty is NEVER modified — always read as user entered\n    qty           = frappe.utils.flt(row.qty)\n    inventory_qty = qty + scheme_qty\n\n    # ── Effective paid quantity (for tax/cost calculation) ───\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    # ── Gross total ──────────────────────────────────────────\n    gross_total       = price * effective_qty\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n    fed_amount        = price * effective_qty * (fed_per / 100.0)\n\n    # ── GST amount ───────────────────────────────────────────\n    if 'exempted' in formula or 'gst exempt' in formula:\n        gst_amount = 0.0\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        gst_amount = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'trade offer' in formula:\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        gst_amount = price * qty * (gst_rate / 100.0)\n    else:\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    # ── Advance Tax amount ───────────────────────────────────\n    if adv_tax_per == 0.0:\n        adv_tax_amount = 0.0\n    elif 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n    elif 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n    elif 'gst' in formula and 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n    elif 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n    else:\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    # ── Line total ───────────────────────────────────────────\n    line_total     = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    # ── Write back calculated values to row ──────────────────\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(valuation_rate, 4)\n    row.amount                    = frappe.utils.flt(line_total, 2)\n\n    # FIX: row.qty is NOT touched — user value stays as entered\n    row.custom_inventory_qty = frappe.utils.flt(inventory_qty, 3)\n    row.valuation_rate       = frappe.utils.flt(valuation_rate, 4)\n\n    # ── Accumulate header totals ─────────────────────────────\n    total_qty     += inventory_qty\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\n# ── Write header summary fields ──────────────────────────────\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception:\n    pass\n\ndoc.total_qty = total_qty",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-14 08:14:27.508578",
  "module": "Aimatic",
  "name": "nabeelpr",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Receipt",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Receipt — Before Save\n# DocType : Purchase Receipt\n# Event   : Before Save\n# ============================================================\n# PURPOSE: Stock receipt with full tax breakdown for data entry.\n#          Stock GL posts NET cost here.\n#          NO tax GL on PR — taxes flow to PI later.\n#          row.rate changed to net unit cost (vendor price in custom_vendor_rate).\n#          Taxes & Charges table NOT rebuilt — taxes show on form but don't post.\n# ============================================================\n\ntotal_gross    = 0.0\ntotal_discount = 0.0\ntotal_trade    = 0.0\ntotal_net      = 0.0\ntotal_gst      = 0.0\ntotal_adv_tax  = 0.0\ntotal_fed      = 0.0\ntotal_line     = 0.0\n\nfor row in doc.items:\n\n    vendor_rate  = float(row.custom_vendor_rate or 0)\n    price        = vendor_rate if vendor_rate else float(row.rate or 0)\n    qty          = float(row.qty or 0)\n    discount_per = float(row.custom_discount_per or 0)\n    trade_offer  = float(row.custom_trade_offer or 0)\n    mrp          = float(row.custom_mrp or 0)\n    gst_rate     = float(row.custom_gst_per or 0)\n    adv_tax_per  = float(row.custom_advance_tax_per or 0)\n    fed_per      = float(row.custom_fed_per or 0)\n    is_3rd       = int(row.custom_is_3rd_schedule or 0)\n\n    gst_formula = 'standard'\n    adv_formula = 'on_tp'\n    has_fed     = 0\n\n    if row.custom_tax_formula:\n        tf = frappe.get_cached_doc('Tax Formula', row.custom_tax_formula)\n        if tf:\n            gst_formula = (tf.gst_formula or 'standard').strip()\n            adv_formula = (tf.advance_tax_formula or 'on_tp').strip()\n            has_fed     = int(tf.has_fed or 0)\n\n    if is_3rd:\n        gst_formula = '3rd_schedule'\n\n    gross_total       = price * qty\n    discount_value    = (price * discount_per / 100.0) * qty\n    trade_offer_total = trade_offer * qty\n\n    fed_amount = 0.0\n    if has_fed and fed_per:\n        fed_amount = price * qty * (fed_per / 100.0)\n\n    gst_amount = 0.0\n    if gst_formula == 'exempted':\n        gst_amount = 0.0\n    elif gst_formula == '3rd_schedule':\n        if mrp and gst_rate:\n            retail_price = mrp / ((100.0 + gst_rate) / 100.0)\n            gst_amount   = (retail_price * gst_rate / 100.0) * qty\n    elif gst_formula == 'after_discount_and_to':\n        unit_taxable = price - (price * discount_per / 100.0) - trade_offer\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\n    elif gst_formula == 'after_discount':\n        unit_taxable = price - (price * discount_per / 100.0)\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\n    elif gst_formula == 'after_to':\n        unit_taxable = price - trade_offer\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\n    else:\n        gst_amount = price * qty * (gst_rate / 100.0)\n\n    adv_tax_amount = 0.0\n    if adv_tax_per > 0 and adv_formula != 'exempted':\n        if adv_formula == 'on_tp_gst_fed_disc_to':\n            base = gross_total - discount_value + gst_amount + fed_amount - trade_offer_total\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_gst_disc_to':\n            base = gross_total - discount_value + gst_amount - trade_offer_total\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_gst_to':\n            base = gross_total - trade_offer_total + gst_amount\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_gst_disc':\n            base = gross_total - discount_value + gst_amount\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_minus_disc':\n            base = gross_total - discount_value\n            adv_tax_amount = adv_tax_per * base / 100.0\n        else:\n            adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    row_net_amount = gross_total - discount_value - trade_offer_total + fed_amount\n    net_unit_cost  = row_net_amount / qty if qty else 0.0\n    line_total     = row_net_amount + gst_amount + adv_tax_amount\n\n    # Display fields\n    row.custom_vendor_rate         = round(price, 4)\n    row.custom_gross_total         = round(gross_total, 2)\n    row.custom_discount_amnt       = round(discount_value, 2)\n    row.custom_trade_offer_total   = round(trade_offer_total, 2)\n    row.custom_gst_amount          = round(gst_amount, 2)\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 2)\n    row.custom_fed_amount          = round(fed_amount, 2)\n    row.custom_line_total          = round(line_total, 2)\n    row.custom_price_without_taxes = round(net_unit_cost, 4)\n    row.custom_price_after_taxes   = round(line_total / qty if qty else 0, 4)\n\n    # PR posts NET cost to Stock GL → use net cost as rate\n    row.rate            = round(net_unit_cost, 4)\n    row.amount          = round(row_net_amount, 2)\n    row.net_rate        = round(net_unit_cost, 4)\n    row.net_amount      = round(row_net_amount, 2)\n    row.base_rate       = round(net_unit_cost, 4)\n    row.base_amount     = round(row_net_amount, 2)\n    row.base_net_rate   = round(net_unit_cost, 4)\n    row.base_net_amount = round(row_net_amount, 2)\n    row.valuation_rate  = round(net_unit_cost, 4)\n    # row.qty NEVER modified\n\n    total_gross    += gross_total\n    total_discount += discount_value\n    total_trade    += trade_offer_total\n    total_net      += row_net_amount\n    total_gst      += gst_amount\n    total_adv_tax  += adv_tax_amount\n    total_fed      += fed_amount\n    total_line     += line_total\n\n\ntotal_gross    = round(total_gross, 2)\ntotal_discount = round(total_discount, 2)\ntotal_trade    = round(total_trade, 2)\ntotal_net      = round(total_net, 2)\ntotal_gst      = round(total_gst, 2)\ntotal_adv_tax  = round(total_adv_tax, 2)\ntotal_fed      = round(total_fed, 2)\ntotal_line     = round(total_line, 2)\n\ndoc.custom_total_gross       = total_gross\ndoc.custom_total_discount    = total_discount\ndoc.custom_total_trade_offer = total_trade\ndoc.custom_total_gst         = total_gst\ndoc.custom_total_advance_tax = total_adv_tax\ndoc.custom_total_fed         = total_fed\ndoc.custom_grand_line_total  = total_line\n\n# PR totals — net only, no taxes in GL\ndoc.total          = total_net\ndoc.net_total      = total_net\ndoc.base_total     = total_net\ndoc.base_net_total = total_net\ndoc.grand_total      = total_net\ndoc.base_grand_total = total_net\ndoc.rounded_total    = round(total_net, 0)\n\n# Clear taxes table — no GL on PR\ndoc.taxes = []\n",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-05 12:16:12.881426",
  "module": null,
  "name": "Auto set the Custom Script On Invoice",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Invoice\n# DocType: Purchase Invoice  |  Event: before_save\n# ============================================================\n# CHANGES FROM PREVIOUS VERSION:\n#   1. inventory_qty = qty + scheme_qty  (full units to stock)\n#   2. valuation_rate = line_total / inventory_qty (landed per unit)\n#   3. row.qty updated to inventory_qty\n#   4. total_qty now counts all units including free ones\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    # ── Read all inputs ──────────────────────────────────────\n    price        = frappe.utils.flt(row.rate)\n    qty          = frappe.utils.flt(row.qty)\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').lower()\n\n    # ── Effective paid quantity (for tax/cost calculation) ───\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    # ── Total inventory quantity (paid + free) ───────────────\n    inventory_qty = qty + scheme_qty\n\n    # ── Gross total ──────────────────────────────────────────\n    gross_total = price * effective_qty\n\n    # ── Discount and trade offer totals ─────────────────────\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n\n    # ── FED amount ───────────────────────────────────────────\n    fed_amount = price * effective_qty * (fed_per / 100.0)\n\n    # ── GST amount ───────────────────────────────────────────\n    if 'exempted' in formula or 'gst exempt' in formula:\n        gst_amount = 0.0\n\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        gst_amount = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n\n    elif 'discount' in formula and 'trade offer' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'discount' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'trade offer' in formula:\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        gst_amount = price * qty * (gst_rate / 100.0)\n\n    else:\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    # ── Advance tax amount ───────────────────────────────────\n    if adv_tax_per == 0.0:\n        adv_tax_amount = 0.0\n\n    elif 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n\n    elif 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n\n    elif 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n\n    elif 'gst' in formula and 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n\n    elif 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n\n    else:\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    # ── Line total ───────────────────────────────────────────\n    line_total = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n\n    # ── Valuation rate: landed cost spread over ALL units ────\n    # Free units absorb their share of the total cost\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    # ── Write back to row ────────────────────────────────────\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(valuation_rate, 4)\n    row.amount                    = frappe.utils.flt(line_total, 2)\n\n    # ── Inventory fields ─────────────────────────────────────\n    row.qty            = frappe.utils.flt(inventory_qty, 3)   # paid + free units to stock\n    row.valuation_rate = frappe.utils.flt(valuation_rate, 4)  # landed cost per unit\n\n    # ── Accumulate header totals ─────────────────────────────\n    total_qty     += inventory_qty   # count all units\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\n# ── Write header summary fields ──────────────────────────────\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception:\n    pass\n\ndoc.total_qty = total_qty",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-05 12:16:12.719853",
  "module": null,
  "name": "Auto Set the Custom Purchase Order",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Order",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Order\n# DocType: Purchase Order  |  Event: before_save\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    price        = frappe.utils.flt(row.rate)\n    qty          = frappe.utils.flt(row.qty)\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').lower()\n\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    inventory_qty = qty + scheme_qty\n\n    gross_total       = price * effective_qty\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n    fed_amount        = price * effective_qty * (fed_per / 100.0)\n\n    if 'exempted' in formula or 'gst exempt' in formula:\n        gst_amount = 0.0\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        gst_amount = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'trade offer' in formula:\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        gst_amount = price * qty * (gst_rate / 100.0)\n    else:\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    if adv_tax_per == 0.0:\n        adv_tax_amount = 0.0\n    elif 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n    elif 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n    elif 'gst' in formula and 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n    elif 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n    else:\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    line_total     = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(valuation_rate, 4)\n    row.amount                    = frappe.utils.flt(line_total, 2)\n\n    row.qty = frappe.utils.flt(inventory_qty, 3)\n    # Note: Purchase Order has no valuation_rate field — only Receipt and Invoice do\n\n    total_qty     += inventory_qty\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception:\n    pass\n\ndoc.total_qty = total_qty",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-10 11:31:33.914973",
  "module": null,
  "name": "Auto Set Accounts and set Inventory",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Invoice\n# DocType : Purchase Invoice\n# Event   : Before Save\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    # ── Read all inputs ──────────────────────────────────────\n    price        = frappe.utils.flt(row.rate)\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').lower()\n\n    # ── FIX: qty is NEVER modified — always read as user entered ──\n    # inventory_qty is stored in custom_inventory_qty (hidden field)\n    # so row.qty stays clean on every save\n    qty           = frappe.utils.flt(row.qty)\n    inventory_qty = qty + scheme_qty\n\n    # ── Effective paid quantity (for tax/cost calculation) ───\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    # ── Gross total ──────────────────────────────────────────\n    gross_total = price * effective_qty\n\n    # ── Discount and trade offer totals ──────────────────────\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n\n    # ── FED amount ───────────────────────────────────────────\n    fed_amount = price * effective_qty * (fed_per / 100.0)\n\n    # ── GST amount ───────────────────────────────────────────\n    if 'exempted' in formula or 'gst exempt' in formula:\n        gst_amount = 0.0\n\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        gst_amount = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n\n    elif 'discount' in formula and 'trade offer' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'discount' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'trade offer' in formula:\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        gst_amount = price * qty * (gst_rate / 100.0)\n\n    else:\n        # Standard GST without scheme\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    # ── Advance Tax amount ───────────────────────────────────\n    if adv_tax_per == 0.0:\n        adv_tax_amount = 0.0\n\n    elif 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n\n    elif 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n\n    elif 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n\n    elif 'gst' in formula and 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n\n    elif 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n\n    else:\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    # ── Line total ───────────────────────────────────────────\n    line_total = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n\n    # ── Valuation rate: landed cost spread over ALL units ────\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    # ── Write back calculated values to row ──────────────────\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(valuation_rate, 4)\n    row.amount                    = frappe.utils.flt(line_total, 2)\n\n    # ── Inventory fields ─────────────────────────────────────\n    # row.qty is NOT touched — user value stays as entered\n    # inventory_qty stored separately for stock purposes\n    row.custom_inventory_qty = frappe.utils.flt(inventory_qty, 3)\n    row.valuation_rate       = frappe.utils.flt(valuation_rate, 4)\n\n    # ── Accumulate header totals ─────────────────────────────\n    total_qty     += inventory_qty\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\n# ── Write header summary fields ──────────────────────────────\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception:\n    pass\n\ndoc.total_qty = total_qty",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "After Submit",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-10 11:31:33.817867",
  "module": null,
  "name": "Auto Create JV for Taxes",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\n# SCRIPT B — on_submit\n# DocType: Purchase Invoice  |  Event: on_submit\n# Creates Journal Entry for GST, Advance Tax, FED, Trade Offer\n# ============================================================\n#\n# Paste this block into a SEPARATE Server Script record.\n# DocType: Purchase Invoice  |  Event: on_submit\n# ============================================================\n \n# Read account heads from Buying Settings\nbuying_settings   = frappe.get_single('Buying Settings')\ngst_account       = buying_settings.get('custom_gst_input_account')\nadv_tax_account   = buying_settings.get('custom_advance_tax_account')\nfed_account       = buying_settings.get('custom_fed_account')\ntrade_offer_acc   = buying_settings.get('custom_trade_offer_account')\n \n# Also allow override from invoice-level fields\ntry:\n    if doc.custom_gst_input_account:\n        gst_account = doc.custom_gst_input_account\nexcept Exception:\n    pass\n \ntry:\n    if doc.custom_advance_tax_account:\n        adv_tax_account = doc.custom_advance_tax_account\nexcept Exception:\n    pass\n \ntry:\n    if doc.custom_fed_account:\n        fed_account = doc.custom_fed_account\nexcept Exception:\n    pass\n \ntry:\n    if doc.custom_trade_offer_account:\n        trade_offer_acc = doc.custom_trade_offer_account\nexcept Exception:\n    pass\n \n# Aggregate totals from items\ntotal_gst       = 0.0\ntotal_adv_tax   = 0.0\ntotal_fed       = 0.0\ntotal_trade_off = 0.0\npayable_account = None\n \nfor row in doc.items:\n    total_gst       += frappe.utils.flt(row.custom_gst_amount)\n    total_adv_tax   += frappe.utils.flt(row.custom_advance_tax_amount)\n    total_fed       += frappe.utils.flt(row.custom_fed_amount)\n    total_trade_off += frappe.utils.flt(row.custom_trade_offer) * frappe.utils.flt(row.custom_scheme_qty + (frappe.utils.flt(row.qty) - frappe.utils.flt(row.custom_scheme_qty)))\n \n# Get the creditor/payable account from the invoice\ntry:\n    payable_account = doc.credit_to\nexcept Exception:\n    payable_account = None\n \n# Only proceed if there is something to post and accounts are set\nhas_gst     = total_gst > 0 and gst_account\nhas_adv_tax = total_adv_tax > 0 and adv_tax_account\nhas_fed     = total_fed > 0 and fed_account\nhas_trade   = total_trade_off > 0 and trade_offer_acc\n \nif payable_account and (has_gst or has_adv_tax or has_fed or has_trade):\n \n    je_entries = []\n    total_debit  = 0.0\n    total_credit = 0.0\n \n    # --- GST Input Tax: Debit GST Account ---\n    if has_gst:\n        je_entries.append({\n            'account':        gst_account,\n            'debit_in_account_currency':  frappe.utils.flt(total_gst, 2),\n            'credit_in_account_currency': 0.0,\n            'against':        doc.supplier,\n            'cost_center':    frappe.db.get_value('Company', doc.company, 'cost_center'),\n            'remarks':        'GST Input Tax — ' + doc.name\n        })\n        total_debit += frappe.utils.flt(total_gst, 2)\n \n    # --- Advance Tax: Debit Advance Tax Receivable ---\n    if has_adv_tax:\n        je_entries.append({\n            'account':        adv_tax_account,\n            'debit_in_account_currency':  frappe.utils.flt(total_adv_tax, 2),\n            'credit_in_account_currency': 0.0,\n            'against':        doc.supplier,\n            'cost_center':    frappe.db.get_value('Company', doc.company, 'cost_center'),\n            'remarks':        'Advance Tax (WHT) — ' + doc.name\n        })\n        total_debit += frappe.utils.flt(total_adv_tax, 2)\n \n    # --- FED: Debit FED Account ---\n    if has_fed:\n        je_entries.append({\n            'account':        fed_account,\n            'debit_in_account_currency':  frappe.utils.flt(total_fed, 2),\n            'credit_in_account_currency': 0.0,\n            'against':        doc.supplier,\n            'cost_center':    frappe.db.get_value('Company', doc.company, 'cost_center'),\n            'remarks':        'FED — ' + doc.name\n        })\n        total_debit += frappe.utils.flt(total_fed, 2)\n \n    # --- Trade Offer: Credit Trade Offer Account (reduction in payable) ---\n    if has_trade:\n        je_entries.append({\n            'account':        trade_offer_acc,\n            'debit_in_account_currency':  0.0,\n            'credit_in_account_currency': frappe.utils.flt(total_trade_off, 2),\n            'against':        doc.supplier,\n            'cost_center':    frappe.db.get_value('Company', doc.company, 'cost_center'),\n            'remarks':        'Vendor Trade Offer — ' + doc.name\n        })\n        total_credit += frappe.utils.flt(total_trade_off, 2)\n \n    # --- Balancing entry against Payable ---\n    # Net debit to payable = GST + ADV TAX + FED - TRADE OFFER\n    net_payable = total_debit - total_credit\n    if net_payable > 0:\n        je_entries.append({\n            'account':        payable_account,\n            'debit_in_account_currency':  0.0,\n            'credit_in_account_currency': frappe.utils.flt(net_payable, 2),\n            'against':        doc.supplier,\n            'cost_center':    frappe.db.get_value('Company', doc.company, 'cost_center'),\n            'party_type':     'Supplier',\n            'party':          doc.supplier,\n            'remarks':        'Tax JV against payable — ' + doc.name\n        })\n    elif net_payable < 0:\n        # Trade offer exceeds taxes — debit payable (reduce amount owed)\n        je_entries.append({\n            'account':        payable_account,\n            'debit_in_account_currency':  frappe.utils.flt(abs(net_payable), 2),\n            'credit_in_account_currency': 0.0,\n            'against':        doc.supplier,\n            'cost_center':    frappe.db.get_value('Company', doc.company, 'cost_center'),\n            'party_type':     'Supplier',\n            'party':          doc.supplier,\n            'remarks':        'Trade Offer credit — ' + doc.name\n        })\n \n    # Create the Journal Entry\n    je = frappe.get_doc({\n        'doctype':          'Journal Entry',\n        'voucher_type':     'Journal Entry',\n        'posting_date':     doc.posting_date,\n        'company':          doc.company,\n        'user_remark':      'Purchase Tax JV for ' + doc.name,\n        'accounts':         je_entries,\n        'multi_currency':   0\n    })\n    je.insert(ignore_permissions=True)\n    je.submit()\n \n    # Link JV back to invoice as a comment so it is easy to find\n    frappe.get_doc({\n        'doctype':      'Comment',\n        'comment_type': 'Info',\n        'reference_doctype': 'Purchase Invoice',\n        'reference_name':    doc.name,\n        'content':      'Tax Journal Entry created: ' + je.name\n    }).insert(ignore_permissions=True)\n ",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-10 11:31:33.300394",
  "module": null,
  "name": "Auto Set Inventory rate",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Order",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Order\n# DocType : Purchase Order\n# Event   : Before Save\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    # ── Read all inputs ──────────────────────────────────────\n    price        = frappe.utils.flt(row.rate)\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').lower()\n\n    # FIX: qty is NEVER modified — always read as user entered\n    qty           = frappe.utils.flt(row.qty)\n    inventory_qty = qty + scheme_qty\n\n    # ── Effective paid quantity (for tax/cost calculation) ───\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    # ── Gross total ──────────────────────────────────────────\n    gross_total       = price * effective_qty\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n    fed_amount        = price * effective_qty * (fed_per / 100.0)\n\n    # ── GST amount ───────────────────────────────────────────\n    if 'exempted' in formula or 'gst exempt' in formula:\n        gst_amount = 0.0\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        gst_amount = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'discount' in formula:\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n    elif 'trade offer' in formula:\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        gst_amount = price * qty * (gst_rate / 100.0)\n    else:\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    # ── Advance Tax amount ───────────────────────────────────\n    if adv_tax_per == 0.0:\n        adv_tax_amount = 0.0\n    elif 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n    elif 'discount' in formula and 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n    elif 'trade offer' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n    elif 'gst' in formula and 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n    elif 'discount' in formula:\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n    else:\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    # ── Line total ───────────────────────────────────────────\n    line_total     = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    # ── Write back calculated values to row ──────────────────\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(valuation_rate, 4)\n    row.amount                    = frappe.utils.flt(line_total, 2)\n\n    # FIX: row.qty is NOT touched — user value stays as entered\n    row.custom_inventory_qty = frappe.utils.flt(inventory_qty, 3)\n\n    # ── Accumulate header totals ─────────────────────────────\n    total_qty     += inventory_qty\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\n# ── Write header summary fields ──────────────────────────────\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception:\n    pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception:\n    pass\n\ndoc.total_qty = total_qty",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-10 16:47:03.582804",
  "module": null,
  "name": "Invoice server script for tax calculation updated",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Invoice\n# DocType : Purchase Invoice\n# Event   : Before Save\n# ============================================================\n# FIELD REFERENCE (from screenshots):\n#   row.qty                      — Accepted Qty (core, user entered, NEVER modified)\n#   row.rate                     — Rate (core)\n#   row.custom_scheme_qty        — Scheme Qty\n#   row.custom_inventory_qty     — Inventory Qty (paid + free, written here)\n#   row.custom_trade_offer       — Trade Offer (per unit)\n#   row.custom_trade_offer_total — Trade Offer Total\n#   row.custom_mrp               — MRP\n#   row.custom_gst_per           — GST %\n#   row.custom_gst_amount        — GST Amount\n#   row.custom_gst_3rd_schedule  — GST 3rd Schedule\n#   row.custom_advance_tax_per   — Advance Tax %\n#   row.custom_advance_tax_amount— Advance Tax Amount\n#   row.custom_fed_per           — FED %\n#   row.custom_fed_amount        — FED Amount\n#   row.custom_gross_total       — Gross Total\n#   row.custom_line_total        — Line Total\n#   row.custom_base_cost_price   — Base Cost Price\n#   row.discount_percentage      — Discount % (core ERPNext)\n#   row.amount                   — Amount (core, updated to line_total)\n#   row.valuation_rate           — Valuation Rate (core, updated to base cost)\n# ============================================================\n\ntotal_gross   = 0.0\ntotal_gst     = 0.0\ntotal_adv_tax = 0.0\ntotal_fed     = 0.0\ntotal_line    = 0.0\ntotal_qty     = 0.0\n\nfor row in doc.items:\n\n    # ── Read inputs — qty NEVER modified ────────────────────\n    price        = frappe.utils.flt(row.rate)\n    qty          = frappe.utils.flt(row.qty)           # user-entered paid qty\n    scheme_qty   = frappe.utils.flt(row.custom_scheme_qty)\n    trade_offer  = frappe.utils.flt(row.custom_trade_offer)\n    mrp          = frappe.utils.flt(row.custom_mrp)\n    gst_rate     = frappe.utils.flt(row.custom_gst_per)\n    adv_tax_per  = frappe.utils.flt(row.custom_advance_tax_per)\n    fed_per      = frappe.utils.flt(row.custom_fed_per)\n    discount_per = frappe.utils.flt(row.discount_percentage)\n    formula      = (row.custom_tax_formula or '').strip().lower()\n\n    # ── Derived quantities ───────────────────────────────────\n    # effective_qty: paid units used for tax base\n    effective_qty = qty - scheme_qty\n    if effective_qty < 0:\n        effective_qty = 0.0\n\n    # inventory_qty: total units going to stock (paid + free)\n    # e.g. Buy 10 Get 5 Free → inventory = 15\n    inventory_qty = qty + scheme_qty\n\n    # ── Derived values ───────────────────────────────────────\n    gross_total       = price * effective_qty\n    discount_value    = (price * discount_per / 100.0) * effective_qty\n    trade_offer_total = trade_offer * effective_qty\n    fed_amount        = price * effective_qty * (fed_per / 100.0)\n\n    # ── GST Calculation (based on Tax Formula name) ──────────\n    gst_3rd_schedule = 0.0\n\n    if 'exempted' in formula or 'gst exempt' in formula:\n        # GST Exempted: 0\n        gst_amount = 0.0\n\n    elif '3rd schedule' in formula or 'third schedule' in formula:\n        # 3rd Schedule: ((MRP / ((100 + GST%) / 100)) x GST% / 100) x effective_qty\n        gst_amount       = ((mrp / ((100.0 + gst_rate) / 100.0)) * (gst_rate / 100.0)) * effective_qty\n        gst_3rd_schedule = gst_amount\n\n    elif 'discount' in formula and 'trade offer' in formula and 'including scheme' in formula:\n        # GST After Discount and Trade Offer (Including Scheme)\n        gst_amount = ((price * qty - price * effective_qty * discount_per / 100.0 - trade_offer * qty) * gst_rate) / 100.0\n\n    elif 'discount' in formula and 'trade offer' in formula:\n        # GST After Discount and Trade Offer (Excluding Scheme)\n        gst_amount = ((price - (price * discount_per / 100.0) - trade_offer) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'discount' in formula and 'trade offer' not in formula:\n        # GST After Discount But Without Trade Offer\n        gst_amount = ((price - (price * discount_per / 100.0)) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'trade offer' in formula and 'discount' not in formula:\n        # GST After Trade Offer But Without Discount\n        gst_amount = ((price - trade_offer) * (gst_rate / 100.0)) * effective_qty\n\n    elif 'with scheme' in formula or 'including scheme' in formula:\n        # GST Standard (With Scheme): Price x QTY x GST% / 100\n        gst_amount = price * qty * (gst_rate / 100.0)\n\n    else:\n        # GST Standard (Without Scheme): Price x effective_qty x GST% / 100\n        gst_amount = price * effective_qty * (gst_rate / 100.0)\n\n    # ── Advance Tax Calculation ──────────────────────────────\n    if adv_tax_per == 0.0 or 'adv exempt' in formula or 'advance tax exempt' in formula:\n        adv_tax_amount = 0.0\n\n    elif 'fed' in formula and 'discount' in formula and 'trade offer' in formula:\n        # Advance Tax on TP + GST + FED - Discount - Trade Offer\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount + fed_amount - trade_offer_total) / 100.0\n\n    elif 'discount' in formula and 'trade offer' in formula:\n        # Advance Tax on TP + GST - Discount - Trade Offer\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount - trade_offer_total) / 100.0\n\n    elif 'trade offer' in formula and 'discount' not in formula:\n        # Advance Tax on TP + GST - Trade Offer\n        adv_tax_amount = adv_tax_per * (gross_total - trade_offer_total + gst_amount) / 100.0\n\n    elif 'gst' in formula and 'discount' in formula:\n        # Advance Tax on TP + GST - Discount\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value + gst_amount) / 100.0\n\n    elif 'discount' in formula:\n        # Advance Tax On TP - Discount\n        adv_tax_amount = adv_tax_per * (gross_total - discount_value) / 100.0\n\n    else:\n        # Advance Tax On TP (standard)\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    # ── Line Total ───────────────────────────────────────────\n    # Line Total = Gross - Discount + GST + AdvTax - TradeOffer + FED\n    line_total = gross_total - discount_value + gst_amount + adv_tax_amount - trade_offer_total + fed_amount\n\n    # ── Valuation Rate (Base Cost Price) ────────────────────\n    # Spread total landed cost across ALL inventory units (paid + free)\n    # This is the true cost per unit in stock\n    valuation_rate = (line_total / inventory_qty) if inventory_qty else 0.0\n\n    # ── Base Cost Price per unit (with FED, without FED) ────\n    # Base Cost Price: (Price - Discount - TradeOffer + FED per unit)\n    base_cost_price = price - (price * discount_per / 100.0) - trade_offer + (fed_amount / qty if qty else 0.0)\n\n    # ── Write back to row fields ─────────────────────────────\n    row.custom_gross_total        = frappe.utils.flt(gross_total, 2)\n    row.custom_trade_offer_total  = frappe.utils.flt(trade_offer_total, 2)\n    row.custom_gst_amount         = frappe.utils.flt(gst_amount, 2)\n    row.custom_gst_3rd_schedule   = frappe.utils.flt(gst_3rd_schedule, 2)\n    row.custom_advance_tax_amount = frappe.utils.flt(adv_tax_amount, 2)\n    row.custom_fed_amount         = frappe.utils.flt(fed_amount, 2)\n    row.custom_line_total         = frappe.utils.flt(line_total, 2)\n    row.custom_base_cost_price    = frappe.utils.flt(base_cost_price, 4)\n    row.custom_inventory_qty      = frappe.utils.flt(inventory_qty, 3)\n\n    # ── Update core ERPNext fields ───────────────────────────\n    row.amount         = frappe.utils.flt(line_total, 2)       # row amount = line total\n    row.net_amount     = frappe.utils.flt(line_total, 2)       # net amount\n    row.valuation_rate = frappe.utils.flt(valuation_rate, 4)   # stock valuation per unit\n    # row.qty is NEVER modified — stays as user entered\n\n    # ── Accumulate totals ────────────────────────────────────\n    total_qty     += inventory_qty\n    total_gross   += gross_total\n    total_gst     += gst_amount\n    total_adv_tax += adv_tax_amount\n    total_fed     += fed_amount\n    total_line    += line_total\n\n# ── Update invoice header custom summary fields ──────────────\ntry:\n    doc.custom_total_gross = frappe.utils.flt(total_gross, 2)\nexcept Exception: pass\n\ntry:\n    doc.custom_total_gst = frappe.utils.flt(total_gst, 2)\nexcept Exception: pass\n\ntry:\n    doc.custom_total_advance_tax = frappe.utils.flt(total_adv_tax, 2)\nexcept Exception: pass\n\ntry:\n    doc.custom_total_fed = frappe.utils.flt(total_fed, 2)\nexcept Exception: pass\n\ntry:\n    doc.custom_grand_line_total = frappe.utils.flt(total_line, 2)\nexcept Exception: pass\n\n# ── Update core ERPNext invoice header fields ────────────────\ndoc.total_qty       = frappe.utils.flt(total_qty, 3)\ndoc.total           = frappe.utils.flt(total_line, 2)   # Net Total shown on invoice\ndoc.net_total       = frappe.utils.flt(total_line, 2)\ndoc.base_total      = frappe.utils.flt(total_line, 2)\ndoc.base_net_total  = frappe.utils.flt(total_line, 2)\ndoc.grand_total     = frappe.utils.flt(total_line, 2)\ndoc.base_grand_total= frappe.utils.flt(total_line, 2)\ndoc.rounded_total   = frappe.utils.flt(round(total_line), 2)",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "After Submit",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-12 02:16:37.119642",
  "module": null,
  "name": "On submit JV creation Script for Invoice",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice\r\n# DocType : Purchase Invoice\r\n# Event   : On Submit\r\n# ============================================================\r\n\r\ntotal_gst     = float(doc.custom_total_gst or 0)\r\ntotal_adv_tax = float(doc.custom_total_advance_tax or 0)\r\ntotal_trade   = sum([float(r.custom_trade_offer_total or 0) for r in doc.items])\r\n\r\nif total_gst == 0 and total_adv_tax == 0 and total_trade == 0:\r\n    pass\r\nelse:\r\n    gst_account   = frappe.db.get_single_value('Buying Settings', 'custom_gst_input_account')\r\n    adv_account   = frappe.db.get_single_value('Buying Settings', 'custom_advance_tax_account')\r\n    trade_account = frappe.db.get_single_value('Buying Settings', 'custom_trade_offer_account')\r\n    tax_clearing  = frappe.db.get_single_value('Buying Settings', 'custom_tax_clearing_account')\r\n    # default_cc    = frappe.db.get_single_value('Global Defaults', 'default_cost_center')\r\n    cost_center   = doc.cost_center or default_cc\r\n\r\n    accounts = []\r\n\r\n    # DR GST Input Account\r\n    if total_gst and gst_account:\r\n        accounts.append({\r\n            'account'                   : gst_account,\r\n            'debit_in_account_currency' : round(total_gst, 2),\r\n            'cost_center'               : cost_center,\r\n            'remarks'                   : f'GST on {doc.name}'\r\n        })\r\n\r\n    # DR Advance Tax Receivable\r\n    if total_adv_tax and adv_account:\r\n        accounts.append({\r\n            'account'                   : adv_account,\r\n            'debit_in_account_currency' : round(total_adv_tax, 2),\r\n            'cost_center'               : cost_center,\r\n            'remarks'                   : f'Advance Tax on {doc.name}'\r\n        })\r\n\r\n    # CR Trade Offer Account\r\n    if total_trade and trade_account:\r\n        accounts.append({\r\n            'account'                    : trade_account,\r\n            'credit_in_account_currency' : round(total_trade, 2),\r\n            'cost_center'                : cost_center,\r\n            'remarks'                    : f'Trade Offer on {doc.name}'\r\n        })\r\n\r\n    # CR Balancing entry — tax clearing account\r\n    balance_cr = round(total_gst + total_adv_tax - total_trade, 2)\r\n    if balance_cr != 0 and tax_clearing:\r\n        accounts.append({\r\n            'account'                    : tax_clearing,\r\n            'credit_in_account_currency' : balance_cr,\r\n            'cost_center'                : cost_center,\r\n            'remarks'                    : f'Tax clearing on {doc.name}'\r\n        })\r\n\r\n    if len(accounts) >= 2:\r\n        jv = frappe.get_doc({\r\n            'doctype'      : 'Journal Entry',\r\n            'voucher_type' : 'Journal Entry',\r\n            'posting_date' : doc.posting_date,\r\n            'company'      : doc.company,\r\n            'user_remark'  : f'Tax JV for Purchase Invoice {doc.name}',\r\n            'accounts'     : accounts\r\n        })\r\n        jv.insert(ignore_permissions=True)\r\n        jv.submit()\r\n        frappe.msgprint(\r\n            f'Tax Journal Entry {jv.name} created.',\r\n            indicator='green',\r\n            alert=True\r\n        )",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-12 01:54:24.169876",
  "module": "Aimatic",
  "name": "nabeelpi",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice\r\n# DocType : Purchase Invoice\r\n# Event   : Before Save\r\n# ============================================================\r\n#\r\n# HOW TO INSTALL:\r\n# 1. Go to: Search bar → \"Server Script\" → + New\r\n# 2. Script Type: DocType Event\r\n# 3. Reference DocType: Purchase Invoice\r\n# 4. DocType Event: Before Save\r\n# 5. Paste this entire script\r\n# 6. Save and Enable\r\n#\r\n# PREREQUISITES:\r\n# - \"Tax Formula\" doctype must exist (see setup guide)\r\n# - All custom fields must be created on Purchase Invoice Item\r\n# - All custom summary fields must be created on Purchase Invoice\r\n#\r\n# ============================================================\r\n# FIELD MAP (Custom Fields on Purchase Invoice Item):\r\n#\r\n#   INPUT FIELDS (user enters these):\r\n#   row.rate                       — TP / Unit Price (core ERPNext)\r\n#   row.qty                        — Total Qty received (core, NEVER modified)\r\n#   row.discount_percentage        — Discount % (core ERPNext)\r\n#   row.custom_trade_offer         — Trade Offer per unit (Currency)\r\n#   row.custom_mrp                 — MRP (Currency, only for 3rd Schedule)\r\n#   row.custom_gst_per             — GST % (Percent)\r\n#   row.custom_advance_tax_per     — Advance Tax % (Percent)\r\n#   row.custom_fed_per             — FED % (Percent)\r\n#   row.custom_tax_formula         — Link to \"Tax Formula\" doctype\r\n#   row.custom_is_3rd_schedule     — Check: Is 3rd Schedule item\r\n#\r\n#   OUTPUT FIELDS (auto-calculated):\r\n#   row.custom_gross_total          — Price × Qty\r\n#   row.custom_discount_value       — Discount amount\r\n#   row.custom_trade_offer_total    — Trade Offer × Qty\r\n#   row.custom_gst_amount           — GST amount\r\n#   row.custom_advance_tax_amount   — Advance Tax amount\r\n#   row.custom_fed_amount           — FED amount\r\n#   row.custom_line_total           — Final row total\r\n#   row.custom_price_without_taxes  — Net cost/unit (inventory rate)\r\n#   row.custom_price_after_taxes    — Landed cost/unit (with all taxes)\r\n#\r\n# INVOICE HEADER FIELDS (Custom Fields on Purchase Invoice):\r\n#   doc.custom_total_gross\r\n#   doc.custom_total_discount\r\n#   doc.custom_total_trade_offer\r\n#   doc.custom_total_gst\r\n#   doc.custom_total_advance_tax\r\n#   doc.custom_total_fed\r\n#   doc.custom_grand_line_total\r\n#\r\n# ============================================================\r\n# TAX FORMULA DOCTYPE — Select field options:\r\n#\r\n# gst_formula options:\r\n#   standard              → Price × QTY × GST% / 100\r\n#   after_discount        → (Price - Price×Disc%/100) × GST%/100 × QTY\r\n#   after_to              → (Price - TradeOffer) × GST%/100 × QTY\r\n#   after_discount_and_to → (Price - Price×Disc%/100 - TO) × GST%/100 × QTY\r\n#   3rd_schedule          → (MRP/((100+GST%)/100)) × GST%/100 × QTY\r\n#   exempted              → 0\r\n#\r\n# advance_tax_formula options:\r\n#   on_tp                    → ADV% × (Price×QTY) / 100\r\n#   on_tp_minus_disc         → ADV% × (Price×QTY - DiscValue) / 100\r\n#   on_tp_gst_disc           → ADV% × (Price×QTY - DiscValue + GST) / 100\r\n#   on_tp_gst_to             → ADV% × (Price×QTY - TO_Total + GST) / 100\r\n#   on_tp_gst_disc_to        → ADV% × (Price×QTY - DiscValue + GST - TO_Total) / 100\r\n#   on_tp_gst_fed_disc_to    → ADV% × (Price×QTY - DiscValue + GST + FED - TO_Total) / 100\r\n#   exempted                 → 0\r\n#\r\n# ============================================================\r\n\r\n# ── Initialize invoice-level totals ──────────────────────────\r\ntotal_gross      = 0.0\r\ntotal_discount   = 0.0\r\ntotal_trade_offer= 0.0\r\ntotal_gst        = 0.0\r\ntotal_adv_tax    = 0.0\r\ntotal_fed        = 0.0\r\ntotal_line       = 0.0\r\n\r\nfor row in doc.items:\r\n\r\n    # ── Read input fields ────────────────────────────────────\r\n    price         = (row.rate)\r\n    qty           = (row.qty)                    # total received, NEVER modified\r\n    trade_offer   = (row.custom_trade_offer)     # per unit\r\n    mrp           = (row.custom_mrp)\r\n    gst_rate      = (row.custom_gst_per)\r\n    adv_tax_per   = (row.custom_advance_tax_per)\r\n    fed_per       = (row.custom_fed_per)\r\n    discount_per  = (row.custom_discount_per)\r\n    is_3rd        = row.custom_is_3rd_schedule\r\n\r\n    # ── Read Tax Formula config from linked doctype ──────────\r\n    gst_formula = 'standard'\r\n    adv_formula = 'on_tp'\r\n    has_fed     = 0\r\n\r\n    if row.custom_tax_formula:\r\n        tf = frappe.get_doc(\r\n            'Tax Formula',\r\n            row.custom_tax_formula\r\n        )\r\n        if tf:\r\n            gst_formula = (tf.get('gst_formula') or 'standard').strip()\r\n            adv_formula = (tf.get('advance_tax_formula') or 'on_tp').strip()\r\n            has_fed     = tf.get('has_fed')\r\n\r\n    # ── Override: if item is 3rd Schedule, force that formula ─\r\n    if is_3rd:\r\n        gst_formula = '3rd_schedule'\r\n\r\n    # ── Derived values ───────────────────────────────────────\r\n    gross_total       = price * qty\r\n    discount_value    = (price * discount_per / 100.0) * qty\r\n    trade_offer_total = trade_offer * qty\r\n\r\n    # ── FED Calculation ──────────────────────────────────────\r\n    # FED applies on all qty. Only calculated if has_fed = 1\r\n    fed_amount = 0.0\r\n    if has_fed and fed_per:\r\n        fed_amount = price * qty * (fed_per / 100.0)\r\n\r\n    # ── GST Calculation ──────────────────────────────────────\r\n    # All GST formulas apply on full QTY (government taxes all goods)\r\n    gst_amount = 0.0\r\n\r\n    if gst_formula == 'exempted':\r\n        gst_amount = 0.0\r\n\r\n    elif gst_formula == '3rd_schedule':\r\n        # 3rd Schedule: ((MRP / ((100 + GST%) / 100)) × GST% / 100) × QTY\r\n        # Reverse-calculates retail price from MRP, then applies GST\r\n        if mrp and gst_rate:\r\n            retail_price = mrp / ((100.0 + gst_rate) / 100.0)\r\n            gst_amount   = (retail_price * gst_rate / 100.0) * qty\r\n        else:\r\n            gst_amount = 0.0\r\n\r\n    elif gst_formula == 'after_discount_and_to':\r\n        # GST on (Price - Discount - Trade Offer) × QTY\r\n        unit_taxable = price - (price * discount_per / 100.0) - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount':\r\n        # GST on (Price - Discount) × QTY\r\n        unit_taxable = price - (price * discount_per / 100.0)\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_to':\r\n        # GST on (Price - Trade Offer) × QTY\r\n        unit_taxable = price - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    else:\r\n        # Standard: Price × QTY × GST% / 100\r\n        gst_amount = round(price * qty * (gst_rate / 100.0),2)\r\n\r\n    # ── Advance Tax Calculation ──────────────────────────────\r\n    # Advance Tax applies on full QTY basis\r\n    adv_tax_amount = 0.0\r\n\r\n    if adv_tax_per == 0.0 or adv_formula == 'exempted':\r\n        adv_tax_amount = 0.0\r\n\r\n    elif adv_formula == 'on_tp_gst_fed_disc_to':\r\n        # ADV% × (Gross - Discount + GST + FED - Trade Offer) / 100\r\n        base = gross_total - discount_value + gst_amount + fed_amount - trade_offer_total\r\n        adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n    elif adv_formula == 'on_tp_gst_disc_to':\r\n        # ADV% × (Gross - Discount + GST - Trade Offer) / 100\r\n        base = gross_total - discount_value + gst_amount - trade_offer_total\r\n        adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n    elif adv_formula == 'on_tp_gst_to':\r\n        # ADV% × (Gross - Trade Offer + GST) / 100\r\n        base = gross_total - trade_offer_total + gst_amount\r\n        adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n    elif adv_formula == 'on_tp_gst_disc':\r\n        # ADV% × (Gross - Discount + GST) / 100\r\n        base = gross_total - discount_value + gst_amount\r\n        adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n    elif adv_formula == 'on_tp_minus_disc':\r\n        # ADV% × (Gross - Discount) / 100\r\n        base = gross_total - discount_value\r\n        adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n    else:\r\n        # Standard: ADV% × Gross / 100\r\n        adv_tax_amount = adv_tax_per * gross_total / 100.0\r\n\r\n    # ── Line Total ───────────────────────────────────────────\r\n    # What you actually pay the vendor for this row\r\n    # Gross - Discount + GST + Advance Tax - Trade Offer + FED\r\n    \r\n    line_total = gross_total - discount_value+ gst_amount+ adv_tax_amount- trade_offer_total+ fed_amount\r\n  \r\n    # ── Price Without Taxes (Inventory Valuation Rate) ───────\r\n    # This is the AVERAGE RATE for stock valuation\r\n    # Includes: Price - Discount - Trade Offer + FED\r\n    # FED is part of cost (not recoverable like GST)\r\n    # Excludes: GST (input tax credit), Advance Tax (receivable)\r\n    price_without_taxes = 0.0\r\n    if qty:\r\n        price_without_taxes = (\r\n            gross_total\r\n            - discount_value\r\n            - trade_offer_total\r\n            + fed_amount\r\n        ) / qty\r\n\r\n    # ── Price After Taxes (Fully Landed Cost) ────────────────\r\n    # Display field for verification — total cost per unit\r\n    price_after_taxes = 0.0\r\n    if qty:\r\n        price_after_taxes = line_total / qty\r\n\r\n    # ── Write calculated values back to row ──────────────────\r\n    row.custom_gross_total         = round(gross_total, 2)\r\n    row.custom_discount_value      = round(discount_value, 2)\r\n    row.custom_discount_amnt       = round(discount_value, 2)\r\n    row.custom_trade_offer_total   = round(trade_offer_total, 2)\r\n    row.custom_gst_amount          = round(gst_amount, 2)\r\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 2)\r\n    row.custom_fed_amount          = round(fed_amount, 2)\r\n    row.custom_line_total          = round(line_total, 2)\r\n    row.custom_price_without_taxes = round(price_without_taxes, 4)\r\n    row.custom_price_after_taxes   = round(price_after_taxes, 4)\r\n\r\n    # ── Update core ERPNext row fields ───────────────────────\r\n    row.amount         = round(line_total, 2)\r\n    row.net_amount     = round(line_total, 2)\r\n    row.valuation_rate = round(price_without_taxes, 4)\r\n    # row.qty is NEVER modified\r\n\r\n    # ── Accumulate invoice totals ────────────────────────────\r\n    total_gross       += gross_total\r\n    total_discount    += discount_value\r\n    total_trade_offer += trade_offer_total\r\n    total_gst         += gst_amount\r\n    total_adv_tax     += adv_tax_amount\r\n    total_fed         += fed_amount\r\n    total_line        += line_total\r\n\r\n\r\n# ── Write invoice header custom totals ───────────────────────\r\ndoc.custom_total_gross       = round(total_gross, 2)\r\ndoc.custom_total_discount    = round(total_discount, 2)\r\ndoc.custom_total_trade_offer = round(total_trade_offer, 2)\r\ndoc.custom_total_gst         = round(total_gst, 2)\r\ndoc.custom_total_advance_tax = round(total_adv_tax, 2)\r\ndoc.custom_total_fed         = round (total_fed, 2)\r\ndoc.custom_grand_line_total  = round(total_line, 2)\r\n\r\n# ── Update core ERPNext invoice totals ───────────────────────\r\ndoc.total            = round(total_line, 2)\r\ndoc.net_total        = round(total_line, 2)\r\ndoc.base_total       = round(total_line, 2)\r\ndoc.base_net_total   = round(total_line, 2)\r\ndoc.grand_total      = round(total_line, 2)\r\ndoc.base_grand_total = round(total_line, 2)\r\ndoc.rounded_total    = round(total_line,2)\r\n",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-12 02:51:20.359317",
  "module": "Aimatic",
  "name": "nabeelpiv2",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice\r\n# DocType : Purchase Invoice\r\n# Event   : Before Save\r\n# Version : 3.0\r\n# ============================================================\r\n\r\n# ── Initialize totals ─────────────────────────────────────────\r\ntotal_net        = 0.0\r\ntotal_gross      = 0.0\r\ntotal_discount   = 0.0\r\ntotal_trade      = 0.0\r\ntotal_gst        = 0.0\r\ntotal_adv_tax    = 0.0\r\ntotal_fed        = 0.0\r\ntotal_line       = 0.0\r\n\r\ngst_account      = None\r\nadv_tax_account  = None\r\n\r\nfor row in doc.items:\r\n\r\n    # ── Read input fields ─────────────────────────────────────\r\n    price        = float(row.rate or 0)\r\n    qty          = float(row.qty or 0)\r\n    discount_per = float(row.custom_discount_per or 0)\r\n    trade_offer  = float(row.custom_trade_offer or 0)\r\n    mrp          = float(row.custom_mrp or 0)\r\n    gst_rate     = float(row.custom_gst_per or 0)\r\n    adv_tax_per  = float(row.custom_advance_tax_per or 0)\r\n    fed_per      = float(row.custom_fed_per or 0)\r\n    is_3rd       = int(row.custom_is_3rd_schedule or 0)\r\n\r\n    # ── Read Tax Formula from linked doctype ──────────────────\r\n    gst_formula = 'standard'\r\n    adv_formula = 'on_tp'\r\n    has_fed     = 0\r\n\r\n    if row.custom_tax_formula:\r\n        tf = frappe.get_cached_doc('Tax Formula', row.custom_tax_formula)\r\n        if tf:\r\n            gst_formula = (tf.gst_formula or 'standard').strip()\r\n            adv_formula = (tf.advance_tax_formula or 'on_tp').strip()\r\n            has_fed     = int(tf.has_fed or 0)\r\n            if not gst_account and tf.gst_account:\r\n                gst_account = tf.gst_account\r\n            if not adv_tax_account and tf.advance_tax_account:\r\n                adv_tax_account = tf.advance_tax_account\r\n\r\n    # ── 3rd Schedule override ─────────────────────────────────\r\n    if is_3rd:\r\n        gst_formula = '3rd_schedule'\r\n\r\n    # ── Derived base values ───────────────────────────────────\r\n    gross_total       = price * qty\r\n    discount_value    = (price * discount_per / 100.0) * qty\r\n    trade_offer_total = trade_offer * qty\r\n\r\n    # ── FED (part of inventory cost) ─────────────────────────\r\n    fed_amount = 0.0\r\n    if has_fed and fed_per:\r\n        fed_amount = price * qty * (fed_per / 100.0)\r\n\r\n    # ── GST Calculation ───────────────────────────────────────\r\n    gst_amount = 0.0\r\n\r\n    if gst_formula == 'exempted':\r\n        gst_amount = 0.0\r\n\r\n    elif gst_formula == '3rd_schedule':\r\n        if mrp and gst_rate:\r\n            retail_price = mrp / ((100.0 + gst_rate) / 100.0)\r\n            gst_amount   = (retail_price * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount_and_to':\r\n        unit_taxable = price - (price * discount_per / 100.0) - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount':\r\n        unit_taxable = price - (price * discount_per / 100.0)\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_to':\r\n        unit_taxable = price - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    else:\r\n        gst_amount = price * qty * (gst_rate / 100.0)\r\n\r\n    # ── Advance Tax Calculation ───────────────────────────────\r\n    adv_tax_amount = 0.0\r\n\r\n    if adv_tax_per > 0 and adv_formula != 'exempted':\r\n\r\n        if adv_formula == 'on_tp_gst_fed_disc_to':\r\n            base = gross_total - discount_value + gst_amount + fed_amount - trade_offer_total\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_disc_to':\r\n            base = gross_total - discount_value + gst_amount - trade_offer_total\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_to':\r\n            base = gross_total - trade_offer_total + gst_amount\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_disc':\r\n            base = gross_total - discount_value + gst_amount\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_minus_disc':\r\n            base = gross_total - discount_value\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        else:\r\n            adv_tax_amount = adv_tax_per * gross_total / 100.0\r\n\r\n    # ── Row Net Amount (cost only — hits inventory) ───────────\r\n    # Gross - Discount - Trade Offer + FED\r\n    row_net_amount = gross_total - discount_value - trade_offer_total + fed_amount\r\n\r\n    # ── Line Total (display only) ─────────────────────────────\r\n    line_total = row_net_amount + gst_amount + adv_tax_amount\r\n\r\n    # ── Valuation Rate ────────────────────────────────────────\r\n    valuation_rate = row_net_amount / qty if qty else 0.0\r\n\r\n    # ── Write back to row ─────────────────────────────────────\r\n    row.custom_gross_total         = round(gross_total, 2)\r\n    row.custom_discount_amnt       = round(discount_value, 2)\r\n    row.custom_trade_offer_total   = round(trade_offer_total, 2)\r\n    row.custom_gst_amount          = round(gst_amount, 2)\r\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 2)\r\n    row.custom_fed_amount          = round(fed_amount, 2)\r\n    row.custom_line_total          = round(line_total, 2)\r\n    row.custom_price_without_taxes = round(valuation_rate, 4)\r\n    row.custom_price_after_taxes   = round(line_total / qty if qty else 0, 4)\r\n\r\n    # ── Core ERPNext row fields ───────────────────────────────\r\n    row.amount         = round(gross_total, 2)\r\n    row.net_rate       = round(row_net_amount / qty if qty else 0, 4)\r\n    row.net_amount     = round(row_net_amount, 2)\r\n    row.valuation_rate = round(valuation_rate, 4)\r\n    # row.qty is NEVER modified\r\n\r\n    # ── Accumulate totals ─────────────────────────────────────\r\n    total_net      += row_net_amount\r\n    total_gross    += gross_total\r\n    total_discount += discount_value\r\n    total_trade    += trade_offer_total\r\n    total_gst      += gst_amount\r\n    total_adv_tax  += adv_tax_amount\r\n    total_fed      += fed_amount\r\n    total_line     += line_total\r\n\r\n\r\n# ── Round accumulated totals ──────────────────────────────────\r\ntotal_net      = round(total_net, 2)\r\ntotal_gross    = round(total_gross, 2)\r\ntotal_discount = round(total_discount, 2)\r\ntotal_trade    = round(total_trade, 2)\r\ntotal_gst      = round(total_gst, 2)\r\ntotal_adv_tax  = round(total_adv_tax, 2)\r\ntotal_fed      = round(total_fed, 2)\r\ntotal_line     = round(total_line, 2)\r\ngrand_total    = round(total_net + total_gst + total_adv_tax, 2)\r\n\r\n# ── Write custom summary fields ───────────────────────────────\r\ndoc.custom_total_gross       = total_gross\r\ndoc.custom_total_discount    = total_discount\r\ndoc.custom_total_trade_offer = total_trade\r\ndoc.custom_total_gst         = total_gst\r\ndoc.custom_total_advance_tax = total_adv_tax\r\ndoc.custom_total_fed         = total_fed\r\ndoc.custom_grand_line_total  = total_line\r\n\r\n# ── Update core ERPNext net total fields ──────────────────────\r\ndoc.total          = total_net\r\ndoc.net_total      = total_net\r\ndoc.base_total     = total_net\r\ndoc.base_net_total = total_net\r\n\r\n# ── Rebuild Taxes and Charges table ──────────────────────────\r\ndoc.taxes = []\r\n\r\n# Row 1: GST\r\nif total_gst and gst_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : gst_account,\r\n        'description'                           : 'GST',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_gst,\r\n        'base_tax_amount'                       : total_gst,\r\n        'tax_amount_after_discount_amount'      : total_gst,\r\n        'base_tax_amount_after_discount_amount' : total_gst,\r\n        'total'                                 : round(total_net + total_gst, 2),\r\n        'base_total'                            : round(total_net + total_gst, 2),\r\n        'cost_center'                           : doc.cost_center\r\n    })\r\n\r\n# Row 2: Advance Tax\r\nif total_adv_tax and adv_tax_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : adv_tax_account,\r\n        'description'                           : 'Advance Tax',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_adv_tax,\r\n        'base_tax_amount'                       : total_adv_tax,\r\n        'tax_amount_after_discount_amount'      : total_adv_tax,\r\n        'base_tax_amount_after_discount_amount' : total_adv_tax,\r\n        'total'                                 : grand_total,\r\n        'base_total'                            : grand_total,\r\n        'cost_center'                           : doc.cost_center\r\n    })\r\n\r\n# ── Update core grand total fields ────────────────────────────\r\ndoc.grand_total      = grand_total\r\ndoc.base_grand_total = grand_total\r\ndoc.rounded_total    = round(grand_total, 0)",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-12 03:13:24.738087",
  "module": "Aimatic",
  "name": "nabeelpiv3",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice\r\n# DocType : Purchase Invoice\r\n# Event   : On Submit\r\n# Version : 2.0 — Final Clean\r\n# ============================================================\r\n#\r\n# Creates Journal Entry for tax GL postings.\r\n# Supplier payable is handled by ERPNext automatically.\r\n# This JV only posts tax accounts that ERPNext doesn't know about.\r\n#\r\n# JV Structure:\r\n#   DR  GST Input Account          (total_gst)\r\n#   DR  Advance Tax Receivable     (total_adv_tax)\r\n#   CR  Tax Clearing Account       (total_gst + total_adv_tax)\r\n#\r\n# Note: Discount and Trade Offer are handled via Taxes & Charges\r\n# table in Before Save — no separate JV entry needed for those.\r\n# ============================================================\r\n\r\ntotal_gst     = float(doc.custom_total_gst or 0)\r\ntotal_adv_tax = float(doc.custom_total_advance_tax or 0)\r\n\r\n# Skip if no tax amounts\r\nif total_gst == 0 and total_adv_tax == 0:\r\n    pass\r\nelse:\r\n    # ── Read accounts from Tax Formula on first item ──────────\r\n    gst_account     = None\r\n    adv_account     = None\r\n    tax_clearing    = None\r\n\r\n    for row in doc.items:\r\n        if row.custom_tax_formula:\r\n            tf = frappe.get_cached_doc('Tax Formula', row.custom_tax_formula)\r\n            if tf:\r\n                if not gst_account and tf.gst_account:\r\n                    gst_account = tf.gst_account\r\n                if not adv_account and tf.advance_tax_account:\r\n                    adv_account = tf.advance_tax_account\r\n                if not tax_clearing and tf.discount_account:\r\n                    tax_clearing = tf.discount_account\r\n        if gst_account and adv_account:\r\n            break\r\n\r\n    default_cc  = frappe.db.get_single_value('Global Defaults', 'default_cost_center')\r\n    cost_center = doc.cost_center or default_cc\r\n\r\n    accounts = []\r\n\r\n    # DR GST Input Account\r\n    if total_gst and gst_account:\r\n        accounts.append({\r\n            'account'                   : gst_account,\r\n            'debit_in_account_currency' : round(total_gst, 2),\r\n            'cost_center'               : cost_center,\r\n            'remarks'                   : f'GST on {doc.name}'\r\n        })\r\n\r\n    # DR Advance Tax Receivable\r\n    if total_adv_tax and adv_account:\r\n        accounts.append({\r\n            'account'                   : adv_account,\r\n            'debit_in_account_currency' : round(total_adv_tax, 2),\r\n            'cost_center'               : cost_center,\r\n            'remarks'                   : f'Advance Tax on {doc.name}'\r\n        })\r\n\r\n    # CR Tax Clearing (balancing entry)\r\n    balance_cr = round(total_gst + total_adv_tax, 2)\r\n    if balance_cr and tax_clearing:\r\n        accounts.append({\r\n            'account'                    : tax_clearing,\r\n            'credit_in_account_currency' : balance_cr,\r\n            'cost_center'                : cost_center,\r\n            'remarks'                    : f'Tax clearing on {doc.name}'\r\n        })\r\n\r\n    if len(accounts) >= 2:\r\n        jv = frappe.get_doc({\r\n            'doctype'      : 'Journal Entry',\r\n            'voucher_type' : 'Journal Entry',\r\n            'posting_date' : doc.posting_date,\r\n            'company'      : doc.company,\r\n            'user_remark'  : f'Tax JV for Purchase Invoice {doc.name}',\r\n            'accounts'     : accounts\r\n        })\r\n        jv.insert(ignore_permissions=True)\r\n        jv.submit()\r\n        frappe.msgprint(\r\n            f'Tax Journal Entry {jv.name} created.',\r\n            indicator='green',\r\n            alert=True\r\n        )",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Submit",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-12 04:01:30.320620",
  "module": "Aimatic",
  "name": "nabeelpisubmit",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice\r\n# DocType : Purchase Invoice\r\n# Event   : On Submit\r\n# Version : 2.0 — Final Clean\r\n# ============================================================\r\n#\r\n# Creates Journal Entry for tax GL postings.\r\n# Supplier payable is handled by ERPNext automatically.\r\n# This JV only posts tax accounts that ERPNext doesn't know about.\r\n#\r\n# JV Structure:\r\n#   DR  GST Input Account          (total_gst)\r\n#   DR  Advance Tax Receivable     (total_adv_tax)\r\n#   CR  Tax Clearing Account       (total_gst + total_adv_tax)\r\n#\r\n# Note: Discount and Trade Offer are handled via Taxes & Charges\r\n# table in Before Save — no separate JV entry needed for those.\r\n# ============================================================\r\n\r\ntotal_gst     = float(doc.custom_total_gst or 0)\r\ntotal_adv_tax = float(doc.custom_total_advance_tax or 0)\r\n\r\n# Skip if no tax amounts\r\nif total_gst == 0 and total_adv_tax == 0:\r\n    pass\r\nelse:\r\n    # ── Read accounts from Tax Formula on first item ──────────\r\n    gst_account     = None\r\n    adv_account     = None\r\n    tax_clearing    = None\r\n\r\n    for row in doc.items:\r\n        if row.custom_tax_formula:\r\n            tf = frappe.get_cached_doc('Tax Formula', row.custom_tax_formula)\r\n            if tf:\r\n                if not gst_account and tf.gst_account:\r\n                    gst_account = tf.gst_account\r\n                if not adv_account and tf.advance_tax_account:\r\n                    adv_account = tf.advance_tax_account\r\n                if not tax_clearing and tf.discount_account:\r\n                    tax_clearing = tf.discount_account\r\n        if gst_account and adv_account:\r\n            break\r\n\r\n    cost_center = doc.cost_center or frappe.get_cached_value('Company', doc.company, 'cost_center')\r\n    \r\n\r\n    accounts = []\r\n\r\n    # DR GST Input Account\r\n    if total_gst and gst_account:\r\n        accounts.append({\r\n            'account'                   : gst_account,\r\n            'debit_in_account_currency' : round(total_gst, 2),\r\n            'cost_center'               : cost_center,\r\n            'remarks'                   : f'GST on {doc.name}'\r\n        })\r\n\r\n    # DR Advance Tax Receivable\r\n    if total_adv_tax and adv_account:\r\n        accounts.append({\r\n            'account'                   : adv_account,\r\n            'debit_in_account_currency' : round(total_adv_tax, 2),\r\n            'cost_center'               : cost_center,\r\n            'remarks'                   : f'Advance Tax on {doc.name}'\r\n        })\r\n\r\n    # CR Tax Clearing (balancing entry)\r\n    balance_cr = round(total_gst + total_adv_tax, 2)\r\n    if balance_cr and tax_clearing:\r\n        accounts.append({\r\n            'account'                    : tax_clearing,\r\n            'credit_in_account_currency' : balance_cr,\r\n            'cost_center'                : cost_center,\r\n            'remarks'                    : f'Tax clearing on {doc.name}'\r\n        })\r\n\r\n    if len(accounts) >= 2:\r\n        jv = frappe.get_doc({\r\n            'doctype'      : 'Journal Entry',\r\n            'voucher_type' : 'Journal Entry',\r\n            'posting_date' : doc.posting_date,\r\n            'company'      : doc.company,\r\n            'user_remark'  : f'Tax JV for Purchase Invoice {doc.name}',\r\n            'accounts'     : accounts\r\n        })\r\n        jv.insert(ignore_permissions=True)\r\n        jv.submit()\r\n        frappe.msgprint(\r\n            f'Tax Journal Entry {jv.name} created.',\r\n            indicator='green',\r\n            alert=True\r\n        )",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-14 08:14:12.458287",
  "module": "Aimatic",
  "name": "nabeelpibefore",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice — FINAL\r\n# DocType : Purchase Invoice\r\n# Event   : Before Save\r\n# ERPNext : v16\r\n# ============================================================\r\n#\r\n# COMPLETE LOGIC (all fixes incorporated):\r\n#\r\n# 1. row.amount AND row.net_amount = net cost (gross - disc - TO + FED)\r\n#    → ERPNext recalculates net_total from row.amount, so both must\r\n#      be net. This was the source of the 47.8 GL imbalance.\r\n# 4. row.rate stays as vendor price (never modified)\r\n# 3. custom_vendor_rate preserves vendor price explicitly\r\n# 4. Taxes & Charges = 4 rows (GST + Advance Tax) → these rows ARE\r\n#    the GL posting. NO On-Submit JV needed (would double-post).\r\n# 5. Accounts read dynamically from Tax Formula doctype\r\n# 6. ERPNext 16: float() not flt(), cost_center from Company\r\n# 7. Discount/Trade Offer absorbed in net amount — no GL row needed\r\n# 8. FED is part of inventory cost (inside net amount)\r\n# 9. is_3rd_schedule checkbox forces 3rd Schedule GST formula\r\n# 10. row.qty NEVER modified\r\n#\r\n# EXPECTED RESULT (example: price 134.7814, qty 14, disc 3%,\r\n#                  MRP 185, GST 18%, 3rd Schedule):\r\n#   gross           = 1,593.38\r\n#   discount        =    47.80\r\n#   net (stock GL)  = 1,545.58\r\n#   GST             =   338.64\r\n#   grand_total     = 1,884.44  → vendor payable ✅\r\n#   valuation_rate  =   148.80 per unit ✅\r\n#\r\n# GL ON SUBMIT (posted by ERPNext automatically):\r\n#   DR Stock/GRNB        = 1,545.58\r\n#   DR Sales Tax on Goods=   338.64   (from taxes row 1)\r\n#   DR Advance Tax       =     0.00   (from taxes row 4 if any)\r\n#   CR Creditors         = 1,884.44   ✅ balanced\r\n#\r\n# PREREQUISITE FIELDS:\r\n#   Purchase Invoice Item:\r\n#     custom_discount_per, custom_discount_amnt, custom_trade_offer,\r\n#     custom_trade_offer_total, custom_mrp, custom_is_3rd_schedule,\r\n#     custom_gst_per, custom_gst_amount, custom_advance_tax_per,\r\n#     custom_advance_tax_amount, custom_fed_per, custom_fed_amount,\r\n#     custom_gross_total, custom_line_total, custom_tax_formula (Link),\r\n#     custom_price_without_taxes, custom_price_after_taxes,\r\n#     custom_vendor_rate (Currency, Read Only)\r\n#   Purchase Invoice:\r\n#     custom_total_gross, custom_total_discount,\r\n#     custom_total_trade_offer, custom_total_gst,\r\n#     custom_total_advance_tax, custom_total_fed,\r\n#     custom_grand_line_total\r\n#   Tax Formula doctype:\r\n#     formula_name, gst_formula (Select), advance_tax_formula (Select),\r\n#     has_fed (Check), gst_account (Link Account),\r\n#     advance_tax_account (Link Account), discount_account (Link Account)\r\n# ============================================================\r\n\r\n# ── Initialize totals ─────────────────────────────────────────\r\ntotal_gross    = 0.0\r\ntotal_discount = 0.0\r\ntotal_trade    = 0.0\r\ntotal_net      = 0.0\r\ntotal_gst      = 0.0\r\ntotal_adv_tax  = 0.0\r\ntotal_fed      = 0.0\r\ntotal_line     = 0.0\r\n\r\ngst_account    = None\r\nadv_account    = None\r\n\r\nfor row in doc.items:\r\n\r\n    # ── Read input fields ─────────────────────────────────────\r\n    # If vendor rate was preserved earlier (re-save), use it as\r\n    # the true vendor price; otherwise row.rate is the vendor price.\r\n    vendor_rate  = float(row.custom_vendor_rate or 0)\r\n    price        = vendor_rate if vendor_rate else float(row.rate or 0)\r\n    qty          = float(row.qty or 0)\r\n    discount_per = float(row.custom_discount_per or 0)\r\n    trade_offer  = float(row.custom_trade_offer or 0)\r\n    mrp          = float(row.custom_mrp or 0)\r\n    gst_rate     = float(row.custom_gst_per or 0)\r\n    adv_tax_per  = float(row.custom_advance_tax_per or 0)\r\n    fed_per      = float(row.custom_fed_per or 0)\r\n    is_3rd       = int(row.custom_is_3rd_schedule or 0)\r\n\r\n    # ── Read Tax Formula config ───────────────────────────────\r\n    gst_formula = 'standard'\r\n    adv_formula = 'on_tp'\r\n    has_fed     = 0\r\n\r\n    if row.custom_tax_formula:\r\n        tf = frappe.get_doc('Tax Formula', row.custom_tax_formula)\r\n        if tf:\r\n            gst_formula = (tf.gst_formula or 'standard').strip()\r\n            adv_formula = (tf.advance_tax_formula or 'on_tp').strip()\r\n            has_fed     = int(tf.has_fed or 0)\r\n            if not gst_account and tf.gst_account:\r\n                gst_account = tf.gst_account\r\n            if not adv_account and tf.advance_tax_account:\r\n                adv_account = tf.advance_tax_account\r\n\r\n    # ── 3rd Schedule override ─────────────────────────────────\r\n    if is_3rd:\r\n        gst_formula = '3rd_schedule'\r\n\r\n    # ── Derived base values ───────────────────────────────────\r\n    gross_total       = price * qty\r\n    discount_value    = (price * discount_per / 100.0) * qty\r\n    trade_offer_total = trade_offer * qty\r\n\r\n    # ── FED (part of inventory cost) ──────────────────────────\r\n    fed_amount = 0.0\r\n    if has_fed and fed_per:\r\n        fed_amount = price * qty * (fed_per / 100.0)\r\n\r\n    # ── GST Calculation ───────────────────────────────────────\r\n    gst_amount = 0.0\r\n\r\n    if gst_formula == 'exempted':\r\n        gst_amount = 0.0\r\n\r\n    elif gst_formula == '3rd_schedule':\r\n        # GST = (MRP / ((100+rate)/100)) * rate/100 * qty\r\n        if mrp and gst_rate:\r\n            retail_price = mrp / ((100.0 + gst_rate) / 100.0)\r\n            gst_amount   = (retail_price * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount_and_to':\r\n        unit_taxable = price - (price * discount_per / 100.0) - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount':\r\n        unit_taxable = price - (price * discount_per / 100.0)\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_to':\r\n        unit_taxable = price - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    else:\r\n        # standard\r\n        gst_amount = price * qty * (gst_rate / 100.0)\r\n\r\n    # ── Advance Tax Calculation ───────────────────────────────\r\n    adv_tax_amount = 0.0\r\n\r\n    if adv_tax_per > 0 and adv_formula != 'exempted':\r\n\r\n        if adv_formula == 'on_tp_gst_fed_disc_to':\r\n            base = gross_total - discount_value + gst_amount + fed_amount - trade_offer_total\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_disc_to':\r\n            base = gross_total - discount_value + gst_amount - trade_offer_total\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_to':\r\n            base = gross_total - trade_offer_total + gst_amount\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_disc':\r\n            base = gross_total - discount_value + gst_amount\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_minus_disc':\r\n            base = gross_total - discount_value\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        else:\r\n            # on_tp\r\n            adv_tax_amount = adv_tax_per * gross_total / 100.0\r\n\r\n    # ── Row Net Amount (drives Stock GL + inventory rate) ─────\r\n    row_net_amount = gross_total - discount_value - trade_offer_total + fed_amount\r\n    net_unit_cost  = row_net_amount / qty if qty else 0.0\r\n\r\n    # ── Line Total (display only — full incl. taxes) ──────────\r\n    line_total = row_net_amount + gst_amount + adv_tax_amount\r\n\r\n    # ── Write display fields ──────────────────────────────────\r\n    row.custom_vendor_rate         = round(price, 4)\r\n    row.custom_gross_total         = round(gross_total, 4)\r\n    row.custom_discount_amnt       = round(discount_value, 4)\r\n    row.custom_trade_offer_total   = round(trade_offer_total, 4)\r\n    row.custom_gst_amount          = round(gst_amount, 4)\r\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 4)\r\n    row.custom_fed_amount          = round(fed_amount, 4)\r\n    row.custom_line_total          = round(line_total, 4)\r\n    row.custom_price_without_taxes = round(net_unit_cost, 4)\r\n    row.custom_price_after_taxes   = round(line_total / qty if qty else 0, 4)\r\n\r\n    # ── Core ERPNext row fields ───────────────────────────────\r\n    # CRITICAL: both amount and net_amount must be NET, because\r\n    # ERPNext recalculates net_total from row.amount after this\r\n    # script runs. rate stays as vendor price.\r\n    row.amount          = round(row_net_amount, 4)\r\n    row.net_amount      = round(row_net_amount, 4)\r\n    row.base_amount     = round(row_net_amount, 4)\r\n    row.base_net_amount = round(row_net_amount, 4)\r\n    row.base_rate       = round(price, 4)\r\n    row.base_net_rate   = round(net_unit_cost, 4)\r\n    row.valuation_rate  = round(net_unit_cost, 4)\r\n    # row.qty  — NEVER modified\r\n    # row.rate — NEVER modified (vendor price)\r\n\r\n    # ── Accumulate totals ─────────────────────────────────────\r\n    total_gross    += gross_total\r\n    total_discount += discount_value\r\n    total_trade    += trade_offer_total\r\n    total_net      += row_net_amount\r\n    total_gst      += gst_amount\r\n    total_adv_tax  += adv_tax_amount\r\n    total_fed      += fed_amount\r\n    total_line     += line_total\r\n\r\n\r\n# ── Round accumulated totals ──────────────────────────────────\r\ntotal_gross    = round(total_gross, 4)\r\ntotal_discount = round(total_discount, 4)\r\ntotal_trade    = round(total_trade, 4)\r\ntotal_net      = round(total_net, 4)\r\ntotal_gst      = round(total_gst, 4)\r\ntotal_adv_tax  = round(total_adv_tax, 4)\r\ntotal_fed      = round(total_fed, 4)\r\ntotal_line     = round(total_line, 4)\r\ngrand_total    = round(total_net + total_gst + total_adv_tax, 4)\r\n\r\n# ── Custom summary fields ─────────────────────────────────────\r\ndoc.custom_total_gross       = total_gross\r\ndoc.custom_total_discount    = total_discount\r\ndoc.custom_total_trade_offer = total_trade\r\ndoc.custom_total_gst         = total_gst\r\ndoc.custom_total_advance_tax = total_adv_tax\r\ndoc.custom_total_fed         = total_fed\r\ndoc.custom_grand_line_total  = total_line\r\n\r\n# ── Core ERPNext totals ───────────────────────────────────────\r\ndoc.total          = total_net\r\ndoc.net_total      = total_net\r\ndoc.base_total     = total_net\r\ndoc.base_net_total = total_net\r\n\r\n# ── Rebuild Taxes and Charges (these rows ARE the GL posting) ─\r\ndoc.taxes = []\r\n\r\ncost_center = doc.cost_center\r\nif not cost_center:\r\n    cost_center = frappe.db.get_value('Company', doc.company, 'cost_center')\r\n\r\n# Row 1: GST (Add)\r\nif total_gst and gst_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : gst_account,\r\n        'description'                           : 'GST',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_gst,\r\n        'base_tax_amount'                       : total_gst,\r\n        'tax_amount_after_discount_amount'      : total_gst,\r\n        'base_tax_amount_after_discount_amount' : total_gst,\r\n        'total'                                 : round(total_net + total_gst, 4),\r\n        'base_total'                            : round(total_net + total_gst, 4),\r\n        'cost_center'                           : cost_center\r\n    })\r\n\r\n# Row 4: Advance Tax (Add)\r\nif total_adv_tax and adv_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : adv_account,\r\n        'description'                           : 'Advance Tax',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_adv_tax,\r\n        'base_tax_amount'                       : total_adv_tax,\r\n        'tax_amount_after_discount_amount'      : total_adv_tax,\r\n        'base_tax_amount_after_discount_amount' : total_adv_tax,\r\n        'total'                                 : grand_total,\r\n        'base_total'                            : grand_total,\r\n        'cost_center'                           : cost_center\r\n    })\r\n\r\n# ── Grand totals ──────────────────────────────────────────────\r\ndoc.grand_total      = grand_total\r\ndoc.base_grand_total = grand_total\r\ndoc.rounded_total    = round(grand_total, 0)\r\n",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-14 08:14:18.676687",
  "module": "Aimatic",
  "name": "nabeelPo",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Order",
  "script": "# ============================================================\n# SERVER SCRIPT — Purchase Order — Before Save\n# DocType : Purchase Order\n# Event   : Before Save\n# ============================================================\n# PURPOSE: Display-only tax calculation for approvals.\n#          No GL posting (PO has no GL).\n#          row.rate stays as vendor price.\n#          row.amount stays as gross (rate × qty) — standard PO behavior.\n#          Custom fields show all tax breakdowns for management visibility.\n# ============================================================\n\ntotal_gross    = 0.0\ntotal_discount = 0.0\ntotal_trade    = 0.0\ntotal_net      = 0.0\ntotal_gst      = 0.0\ntotal_adv_tax  = 0.0\ntotal_fed      = 0.0\ntotal_line     = 0.0\n\nfor row in doc.items:\n\n    vendor_rate  = float(row.custom_vendor_rate or 0)\n    price        = vendor_rate if vendor_rate else float(row.rate or 0)\n    qty          = float(row.qty or 0)\n    discount_per = float(row.custom_discount_per or 0)\n    trade_offer  = float(row.custom_trade_offer or 0)\n    mrp          = float(row.custom_mrp or 0)\n    gst_rate     = float(row.custom_gst_per or 0)\n    adv_tax_per  = float(row.custom_advance_tax_per or 0)\n    fed_per      = float(row.custom_fed_per or 0)\n    is_3rd       = int(row.custom_is_3rd_schedule or 0)\n\n    gst_formula = 'standard'\n    adv_formula = 'on_tp'\n    has_fed     = 0\n\n    if row.custom_tax_formula:\n        tf = frappe.get_cached_doc('Tax Formula', row.custom_tax_formula)\n        if tf:\n            gst_formula = (tf.gst_formula or 'standard').strip()\n            adv_formula = (tf.advance_tax_formula or 'on_tp').strip()\n            has_fed     = int(tf.has_fed or 0)\n\n    if is_3rd:\n        gst_formula = '3rd_schedule'\n\n    gross_total       = price * qty\n    discount_value    = (price * discount_per / 100.0) * qty\n    trade_offer_total = trade_offer * qty\n\n    fed_amount = 0.0\n    if has_fed and fed_per:\n        fed_amount = price * qty * (fed_per / 100.0)\n\n    # GST\n    gst_amount = 0.0\n    if gst_formula == 'exempted':\n        gst_amount = 0.0\n    elif gst_formula == '3rd_schedule':\n        if mrp and gst_rate:\n            retail_price = mrp / ((100.0 + gst_rate) / 100.0)\n            gst_amount   = (retail_price * gst_rate / 100.0) * qty\n    elif gst_formula == 'after_discount_and_to':\n        unit_taxable = price - (price * discount_per / 100.0) - trade_offer\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\n    elif gst_formula == 'after_discount':\n        unit_taxable = price - (price * discount_per / 100.0)\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\n    elif gst_formula == 'after_to':\n        unit_taxable = price - trade_offer\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\n    else:\n        gst_amount = price * qty * (gst_rate / 100.0)\n\n    # Advance Tax\n    adv_tax_amount = 0.0\n    if adv_tax_per > 0 and adv_formula != 'exempted':\n        if adv_formula == 'on_tp_gst_fed_disc_to':\n            base = gross_total - discount_value + gst_amount + fed_amount - trade_offer_total\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_gst_disc_to':\n            base = gross_total - discount_value + gst_amount - trade_offer_total\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_gst_to':\n            base = gross_total - trade_offer_total + gst_amount\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_gst_disc':\n            base = gross_total - discount_value + gst_amount\n            adv_tax_amount = adv_tax_per * base / 100.0\n        elif adv_formula == 'on_tp_minus_disc':\n            base = gross_total - discount_value\n            adv_tax_amount = adv_tax_per * base / 100.0\n        else:\n            adv_tax_amount = adv_tax_per * gross_total / 100.0\n\n    row_net_amount = gross_total - discount_value - trade_offer_total + fed_amount\n    net_unit_cost  = row_net_amount / qty if qty else 0.0\n    line_total     = row_net_amount + gst_amount + adv_tax_amount\n\n    # Write display fields (PO is display-only — rate stays as vendor price)\n    row.custom_vendor_rate         = round(price, 4)\n    row.custom_gross_total         = round(gross_total, 2)\n    row.custom_discount_amnt       = round(discount_value, 2)\n    row.custom_trade_offer_total   = round(trade_offer_total, 2)\n    row.custom_gst_amount          = round(gst_amount, 2)\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 2)\n    row.custom_fed_amount          = round(fed_amount, 2)\n    row.custom_line_total          = round(line_total, 2)\n    row.custom_price_without_taxes = round(net_unit_cost, 4)\n    row.custom_price_after_taxes   = round(line_total / qty if qty else 0, 4)\n\n    # PO row.amount stays gross — no GL, just commitment\n    # row.qty and row.rate NEVER modified\n\n    total_gross    += gross_total\n    total_discount += discount_value\n    total_trade    += trade_offer_total\n    total_net      += row_net_amount\n    total_gst      += gst_amount\n    total_adv_tax  += adv_tax_amount\n    total_fed      += fed_amount\n    total_line     += line_total\n\n\ntotal_gross    = round(total_gross, 2)\ntotal_discount = round(total_discount, 2)\ntotal_trade    = round(total_trade, 2)\ntotal_net      = round(total_net, 2)\ntotal_gst      = round(total_gst, 2)\ntotal_adv_tax  = round(total_adv_tax, 2)\ntotal_fed      = round(total_fed, 2)\ntotal_line     = round(total_line, 2)\n\ndoc.custom_total_gross       = total_gross\ndoc.custom_total_discount    = total_discount\ndoc.custom_total_trade_offer = total_trade\ndoc.custom_total_gst         = total_gst\ndoc.custom_total_advance_tax = total_adv_tax\ndoc.custom_total_fed         = total_fed\ndoc.custom_grand_line_total  = total_line\n",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-14 08:14:02.077237",
  "module": "Aimatic",
  "name": "piv2",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice — FINAL WORKING\r\n# DocType : Purchase Invoice\r\n# Event   : Before Save\r\n# ERPNext : v16\r\n# ============================================================\r\n#\r\n# CRITICAL — row.rate MUST be set to net_unit_cost.\r\n# ERPNext v16 forces amount = rate × qty on submit validation.\r\n# Vendor price preserved in custom_vendor_rate for print formats.\r\n#\r\n# GL on submit (balanced):\r\n#   DR Stock/GRNB        = net_total\r\n#   DR GST account       = total_gst\r\n#   DR Advance Tax acct  = total_adv_tax\r\n#   CR Creditors         = grand_total ✅\r\n# ============================================================\r\n\r\n# ── Initialize totals ─────────────────────────────────────────\r\ntotal_gross    = 0.0\r\ntotal_discount = 0.0\r\ntotal_trade    = 0.0\r\ntotal_net      = 0.0\r\ntotal_gst      = 0.0\r\ntotal_adv_tax  = 0.0\r\ntotal_fed      = 0.0\r\ntotal_line     = 0.0\r\n\r\ngst_account    = None\r\nadv_account    = None\r\n\r\nfor row in doc.items:\r\n\r\n    # ── Read inputs ───────────────────────────────────────────\r\n    # Use custom_vendor_rate if previously stored (re-save case)\r\n    # otherwise row.rate IS the vendor price on first save\r\n    vendor_rate  = float(row.custom_vendor_rate or 0)\r\n    price        = vendor_rate if vendor_rate else float(row.rate or 0)\r\n    qty          = float(row.qty or 0)\r\n    discount_per = float(row.custom_discount_per or 0)\r\n    trade_offer  = float(row.custom_trade_offer or 0)\r\n    mrp          = float(row.custom_mrp or 0)\r\n    gst_rate     = float(row.custom_gst_per or 0)\r\n    adv_tax_per  = float(row.custom_advance_tax_per or 0)\r\n    fed_per      = float(row.custom_fed_per or 0)\r\n    is_3rd       = int(row.custom_is_3rd_schedule or 0)\r\n\r\n    # ── Read Tax Formula config ───────────────────────────────\r\n    gst_formula = 'standard'\r\n    adv_formula = 'on_tp'\r\n    has_fed     = 0\r\n\r\n    if row.custom_tax_formula:\r\n        tf = frappe.get_doc('Tax Formula', row.custom_tax_formula)\r\n        if tf:\r\n            gst_formula = (tf.gst_formula or 'standard').strip()\r\n            adv_formula = (tf.advance_tax_formula or 'on_tp').strip()\r\n            has_fed     = int(tf.has_fed or 0)\r\n            if not gst_account and tf.gst_account:\r\n                gst_account = tf.gst_account\r\n            if not adv_account and tf.advance_tax_account:\r\n                adv_account = tf.advance_tax_account\r\n\r\n    # ── 3rd Schedule override ─────────────────────────────────\r\n    if is_3rd:\r\n        gst_formula = '3rd_schedule'\r\n\r\n    # ── Derived base values ───────────────────────────────────\r\n    gross_total       = price * qty\r\n    discount_value    = (price * discount_per / 100.0) * qty\r\n    trade_offer_total = trade_offer * qty\r\n\r\n    # ── FED (part of inventory cost) ──────────────────────────\r\n    fed_amount = 0.0\r\n    if has_fed and fed_per:\r\n        fed_amount = price * qty * (fed_per / 100.0)\r\n\r\n    # ── GST Calculation ───────────────────────────────────────\r\n    gst_amount = 0.0\r\n\r\n    if gst_formula == 'exempted':\r\n        gst_amount = 0.0\r\n\r\n    elif gst_formula == '3rd_schedule':\r\n        if mrp and gst_rate:\r\n            retail_price = mrp / ((100.0 + gst_rate) / 100.0)\r\n            gst_amount   = (retail_price * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount_and_to':\r\n        unit_taxable = price - (price * discount_per / 100.0) - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_discount':\r\n        unit_taxable = price - (price * discount_per / 100.0)\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    elif gst_formula == 'after_to':\r\n        unit_taxable = price - trade_offer\r\n        gst_amount   = (unit_taxable * gst_rate / 100.0) * qty\r\n\r\n    else:\r\n        gst_amount = price * qty * (gst_rate / 100.0)\r\n\r\n    # ── Advance Tax Calculation ───────────────────────────────\r\n    adv_tax_amount = 0.0\r\n\r\n    if adv_tax_per > 0 and adv_formula != 'exempted':\r\n\r\n        if adv_formula == 'on_tp_gst_fed_disc_to':\r\n            base = gross_total - discount_value + gst_amount + fed_amount - trade_offer_total\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_disc_to':\r\n            base = gross_total - discount_value + gst_amount - trade_offer_total\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_to':\r\n            base = gross_total - trade_offer_total + gst_amount\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_gst_disc':\r\n            base = gross_total - discount_value + gst_amount\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        elif adv_formula == 'on_tp_minus_disc':\r\n            base = gross_total - discount_value\r\n            adv_tax_amount = adv_tax_per * base / 100.0\r\n\r\n        else:\r\n            adv_tax_amount = adv_tax_per * gross_total / 100.0\r\n\r\n    # ── Row Net Amount (drives Stock GL + inventory rate) ─────\r\n    row_net_amount = gross_total - discount_value - trade_offer_total + fed_amount\r\n    net_unit_cost  = row_net_amount / qty if qty else 0.0\r\n\r\n    # ── Line Total (display only — full incl. taxes) ──────────\r\n    line_total = row_net_amount + gst_amount + adv_tax_amount\r\n\r\n    # ── Write display fields ──────────────────────────────────\r\n    row.custom_vendor_rate         = round(price, 4)\r\n    row.custom_gross_total         = round(gross_total, 4)\r\n    row.custom_discount_amnt       = round(discount_value, 4)\r\n    row.custom_trade_offer_total   = round(trade_offer_total, 4)\r\n    row.custom_gst_amount          = round(gst_amount, 4)\r\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 4)\r\n    row.custom_fed_amount          = round(fed_amount, 4)\r\n    row.custom_line_total          = round(line_total, 4)\r\n    row.custom_price_without_taxes = round(net_unit_cost, 4)\r\n    row.custom_price_after_taxes   = round(line_total / qty if qty else 0, 4)\r\n\r\n    # ── Core ERPNext row fields ───────────────────────────────\r\n    # CRITICAL: rate MUST be net_unit_cost so ERPNext's\r\n    # amount = rate × qty calculation produces correct GL.\r\n    # Vendor price preserved in custom_vendor_rate for print formats.\r\n    row.rate            = round(net_unit_cost, 4)\r\n    row.amount          = round(row_net_amount, 4)\r\n    row.net_rate        = round(net_unit_cost, 4)\r\n    row.net_amount      = round(row_net_amount, 4)\r\n    row.base_rate       = round(net_unit_cost, 4)\r\n    row.base_amount     = round(row_net_amount, 4)\r\n    row.base_net_rate   = round(net_unit_cost, 4)\r\n    row.base_net_amount = round(row_net_amount, 4)\r\n    row.valuation_rate  = round(net_unit_cost, 4)\r\n    # row.qty NEVER modified\r\n\r\n    # ── Accumulate totals ─────────────────────────────────────\r\n    total_gross    += gross_total\r\n    total_discount += discount_value\r\n    total_trade    += trade_offer_total\r\n    total_net      += row_net_amount\r\n    total_gst      += gst_amount\r\n    total_adv_tax  += adv_tax_amount\r\n    total_fed      += fed_amount\r\n    total_line     += line_total\r\n\r\n\r\n# ── Round accumulated totals ──────────────────────────────────\r\ntotal_gross    = round(total_gross, 4)\r\ntotal_discount = round(total_discount, 4)\r\ntotal_trade    = round(total_trade, 4)\r\ntotal_net      = round(total_net, 4)\r\ntotal_gst      = round(total_gst, 4)\r\ntotal_adv_tax  = round(total_adv_tax, 4)\r\ntotal_fed      = round(total_fed, 4)\r\ntotal_line     = round(total_line, 4)\r\ngrand_total    = round(total_net + total_gst + total_adv_tax, 4)\r\n\r\n# ── Custom summary fields ─────────────────────────────────────\r\ndoc.custom_total_gross       = total_gross\r\ndoc.custom_total_discount    = total_discount\r\ndoc.custom_total_trade_offer = total_trade\r\ndoc.custom_total_gst         = total_gst\r\ndoc.custom_total_advance_tax = total_adv_tax\r\ndoc.custom_total_fed         = total_fed\r\ndoc.custom_grand_line_total  = total_line\r\n\r\n# ── Core ERPNext totals ───────────────────────────────────────\r\ndoc.total          = total_net\r\ndoc.net_total      = total_net\r\ndoc.base_total     = total_net\r\ndoc.base_net_total = total_net\r\n\r\n# ── Rebuild Taxes and Charges (these rows ARE the GL posting) ─\r\ndoc.taxes = []\r\n\r\ncost_center = doc.cost_center\r\nif not cost_center:\r\n    cost_center = frappe.db.get_value('Company', doc.company, 'cost_center')\r\n\r\n# Row 1: GST (Add)\r\nif total_gst and gst_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : gst_account,\r\n        'description'                           : 'GST',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_gst,\r\n        'base_tax_amount'                       : total_gst,\r\n        'tax_amount_after_discount_amount'      : total_gst,\r\n        'base_tax_amount_after_discount_amount' : total_gst,\r\n        'total'                                 : round(total_net + total_gst, 4),\r\n        'base_total'                            : round(total_net + total_gst, 4),\r\n        'cost_center'                           : cost_center\r\n    })\r\n\r\n# Row 2: Advance Tax (Add)\r\nif total_adv_tax and adv_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : adv_account,\r\n        'description'                           : 'Advance Tax',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_adv_tax,\r\n        'base_tax_amount'                       : total_adv_tax,\r\n        'tax_amount_after_discount_amount'      : total_adv_tax,\r\n        'base_tax_amount_after_discount_amount' : total_adv_tax,\r\n        'total'                                 : grand_total,\r\n        'base_total'                            : grand_total,\r\n        'cost_center'                           : cost_center\r\n    })\r\n\r\n# ── Grand totals ──────────────────────────────────────────────\r\ndoc.grand_total      = grand_total\r\ndoc.base_grand_total = grand_total\r\ndoc.rounded_total    = round(grand_total, 0)",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-15 09:33:35.884018",
  "module": "Aimatic",
  "name": "pitest",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Invoice — DYNAMIC ENGINE v2\r\n# DocType : Purchase Invoice\r\n# Event   : Before Save\r\n# ERPNext : v16\r\n# ============================================================\r\n#\r\n# Variable namespace in formulas = actual ERPNext field names\r\n# (lowercase, with custom_ prefix). No translation layer.\r\n#\r\n# Formulas are stored in Tax Formula doctype as math strings\r\n# and evaluated via frappe.safe_eval.\r\n#\r\n# AVAILABLE VARIABLES IN FORMULAS:\r\n#   rate                          — Vendor price (from row.rate)\r\n#   qty                           — Total quantity received\r\n#   custom_scheme_qty             — Free units (scheme)\r\n#   custom_discount_per           — Discount %\r\n#   custom_trade_offer            — Trade Offer per unit\r\n#   custom_mrp                    — MRP\r\n#   custom_gst_per                — GST %\r\n#   custom_advance_tax_per        — Advance Tax %\r\n#   custom_fed_per                — FED %\r\n#   custom_gross_total            — Rate × Qty (auto-computed)\r\n#   custom_discount_amnt          — Discount value (auto-computed)\r\n#   custom_trade_offer_total      — Trade Offer total (auto-computed)\r\n#   custom_gst_amount             — GST result (available after GST evaluates)\r\n#   custom_advance_tax_amount     — Advance Tax (available after eval)\r\n#   custom_fed_amount             — FED (available after FED evaluates)\r\n# ============================================================\r\n\r\n# ============================================================\r\n# ERPNext v16 SERVER SCRIPT SAFE FORMULA EVALUATOR\r\n# No eval()\r\n# No safe_eval()\r\n# Supports + - * / () and variables\r\n# ============================================================\r\n\r\n# ============================================================\r\n# ERPNext v16 SERVER SCRIPT FORMULA ENGINE\r\n# Supports: + - * / ()\r\n# ============================================================\r\n\r\ndef evaluate_formula(formula_name, namespace):\r\n\r\n    if not formula_name:\r\n        return 0.0\r\n\r\n    try:\r\n\r\n        tf = frappe.get_doc(\"Tax Formula\", formula_name)\r\n\r\n        expr = (tf.formula_string or \"\").strip()\r\n\r\n        if not expr:\r\n            return 0.0\r\n\r\n        # replace variables\r\n        for k in sorted(namespace, key=len, reverse=True):\r\n\r\n            try:\r\n                val = float(namespace[k] or 0)\r\n            except:\r\n                val = 0\r\n\r\n            expr = expr.replace(k, str(val))\r\n\r\n        # remove spaces\r\n        expr = expr.replace(\" \", \"\")\r\n\r\n        # recursive calculator\r\n        def calc(e):\r\n\r\n            while \"(\" in e:\r\n                start = e.rfind(\"(\")\r\n                end = e.find(\")\", start)\r\n\r\n                inner = calc(e[start+1:end])\r\n\r\n                e = e[:start] + str(inner) + e[end+1:]\r\n\r\n            tokens = []\r\n            num = \"\"\r\n\r\n            for c in e:\r\n                if c in \"+-*/\":\r\n                    if num:\r\n                        tokens.append(float(num))\r\n                    tokens.append(c)\r\n                    num = \"\"\r\n                else:\r\n                    num += c\r\n\r\n            if num:\r\n                tokens.append(float(num))\r\n\r\n            i = 0\r\n            while i < len(tokens):\r\n                if tokens[i] == \"*\":\r\n                    tokens[i-1:i+2] = [tokens[i-1] * tokens[i+1]]\r\n                    i = 0\r\n                elif tokens[i] == \"/\":\r\n                    tokens[i-1:i+2] = [tokens[i-1] / tokens[i+1]]\r\n                    i = 0\r\n                else:\r\n                    i += 1\r\n\r\n            result = tokens[0]\r\n\r\n            i = 1\r\n\r\n            while i < len(tokens):\r\n                if tokens[i] == \"+\":\r\n                    result += tokens[i+1]\r\n\r\n                elif tokens[i] == \"-\":\r\n                    result -= tokens[i+1]\r\n\r\n                i += 2\r\n\r\n            return result\r\n\r\n        return round(float(calc(expr)), 4)\r\n\r\n    except Exception as e:\r\n\r\n        frappe.msgprint(\r\n            f\"\"\"\r\n            Formula: {formula_name}<br>\r\n            Expression: {expr}<br>\r\n            Error: {str(e)}\r\n            \"\"\",\r\n            title=\"Formula Error\",\r\n            indicator=\"red\"\r\n        )\r\n\r\n        return 0.0\r\n\r\ntotal_gross    = 0.0\r\ntotal_discount = 0.0\r\ntotal_trade    = 0.0\r\ntotal_net      = 0.0\r\ntotal_gst      = 0.0\r\ntotal_adv_tax  = 0.0\r\ntotal_fed      = 0.0\r\ntotal_line     = 0.0\r\n\r\ngst_account    = None\r\nadv_account    = None\r\n\r\n\r\nfor row in doc.items:\r\n    # ── Sync row settings back to Item master ─────────────────\r\n    if row.item_code:\r\n        item_updates = {}\r\n        \r\n        # Sync 3rd Schedule status\r\n        item_is_3rd = frappe.db.get_value('Item', row.item_code, 'custom_is_3rd_schedule')\r\n        if int(row.custom_is_3rd_schedule or 0) != int(item_is_3rd or 0):\r\n            item_updates['custom_is_3rd_schedule'] = int(row.custom_is_3rd_schedule or 0)\r\n        \r\n        # Sync MRP\r\n        if row.custom_mrp and float(row.custom_mrp) > 0:\r\n            item_mrp = frappe.db.get_value('Item', row.item_code, 'custom_mrp')\r\n            if float(row.custom_mrp) != float(item_mrp or 0):\r\n                item_updates['custom_mrp'] = float(row.custom_mrp)\r\n        \r\n        # Apply updates if any\r\n        if item_updates:\r\n            frappe.db.set_value('Item', row.item_code, item_updates)\r\n\r\n    # ── Read inputs ───────────────────────────────────────────\r\n    vendor_rate  = float(row.custom_vendor_rate or 0)\r\n    price        = vendor_rate if vendor_rate else float(row.rate or 0)\r\n    qty          = float(row.qty or 0)\r\n    scheme_qty   = float(row.custom_scheme_qty or 0)\r\n    discount_per = float(row.custom_discount_per or 0)\r\n    trade_offer  = float(row.custom_trade_offer or 0)\r\n    mrp          = float(row.custom_mrp or 0)\r\n    gst_rate     = float(row.custom_gst_per or 0)\r\n    adv_tax_per  = float(row.custom_advance_tax_per or 0)\r\n    fed_per      = float(row.custom_fed_per or 0)\r\n    is_3rd       = int(row.custom_is_3rd_schedule or 0)\r\n\r\n    # ── Derived values ────────────────────────────────────────\r\n    paid_qty          = qty - scheme_qty\r\n    gross_total       = price * qty\r\n    discount_value    = (price * discount_per / 100.0) * paid_qty\r\n    trade_offer_total = trade_offer * paid_qty\r\n\r\n    # ── Build namespace using ERPNext field names ─────────────\r\n    ns = {\r\n        'rate'                      : price,\r\n        'qty'                       : qty,\r\n        'custom_scheme_qty'         : scheme_qty,\r\n        'custom_discount_per'       : discount_per,\r\n        'custom_trade_offer'        : trade_offer,\r\n        'custom_mrp'                : mrp,\r\n        'custom_gst_per'            : gst_rate,\r\n        'custom_advance_tax_per'    : adv_tax_per,\r\n        'custom_fed_per'            : fed_per,\r\n        'custom_gross_total'        : gross_total,\r\n        'custom_discount_amnt'      : discount_value,\r\n        'custom_trade_offer_total'  : trade_offer_total,\r\n        'custom_gst_amount'         : 0.0,\r\n        'custom_advance_tax_amount' : 0.0,\r\n        'custom_fed_amount'         : 0.0,\r\n    }\r\n\r\n    # ── Resolve formulas ──────────────────────────────────────\r\n    gst_formula_name = row.custom_gst_formula or ''\r\n    adv_formula_name = row.custom_advance_tax_formula or ''\r\n    fed_formula_name = row.custom_fed_formula or ''\r\n\r\n    # 3rd Schedule override\r\n    if is_3rd and not gst_formula_name.lower().startswith('3rd'):\r\n        match = frappe.db.get_value(\r\n            'Tax Formula',\r\n            {'formula_name': ['like', '%3rd Schedule%'], 'formula_type': 'gst'},\r\n            'name'\r\n        )\r\n        if match:\r\n            gst_formula_name = match\r\n\r\n    # ── Capture GL accounts ───────────────────────────────────\r\n    if gst_formula_name and not gst_account:\r\n        gst_account = frappe.db.get_value('Tax Formula', gst_formula_name, 'gst_account')\r\n    if adv_formula_name and not adv_account:\r\n        adv_account = frappe.db.get_value('Tax Formula', adv_formula_name, 'advance_tax_account')\r\n\r\n    # ── Evaluate FED first ────────────────────────────────────\r\n    if fed_formula_name:\r\n        fed_amount = evaluate_formula(fed_formula_name, ns)\r\n    elif fed_per:\r\n        fed_amount = price * paid_qty * (fed_per / 100.0)\r\n    else:\r\n        fed_amount = 0.0\r\n    ns['custom_fed_amount'] = fed_amount\r\n\r\n    # ── Evaluate GST ──────────────────────────────────────────\r\n    gst_amount = evaluate_formula(gst_formula_name, ns) if gst_formula_name else 0.0\r\n    ns['custom_gst_amount'] = gst_amount \r\n    \r\n\r\n    # ── Evaluate Advance Tax ──────────────────────────────────\r\n    adv_tax_amount = evaluate_formula(adv_formula_name, ns) if adv_formula_name else 0.0\r\n    ns['custom_advance_tax_amount'] = adv_tax_amount\r\n\r\n    # ── Row totals ────────────────────────────────────────────\r\n    row_net_amount = gross_total - discount_value - trade_offer_total + fed_amount\r\n    net_unit_cost  = row_net_amount / qty if qty else 0.0\r\n    line_total     = row_net_amount + gst_amount + adv_tax_amount\r\n\r\n    # ── Write display fields ──────────────────────────────────\r\n    row.custom_vendor_rate         = round(price, 4)\r\n    row.custom_gross_total         = round(gross_total, 4)\r\n    row.custom_discount_amnt       = round(discount_value, 4)\r\n    row.custom_trade_offer_total   = round(trade_offer_total, 4)\r\n    row.custom_gst_amount          = round(gst_amount, 4)\r\n    row.custom_advance_tax_amount  = round(adv_tax_amount, 4)\r\n    row.custom_fed_amount          = round(fed_amount, 4)\r\n    row.custom_line_total          = round(line_total, 4)\r\n    row.custom_price_without_taxes = round(net_unit_cost, 4)\r\n    row.custom_price_after_taxes   = round(line_total / qty if qty else 0, 4)\r\n\r\n    # ── Core ERPNext fields ───────────────────────────────────\r\n    row.rate            = round(net_unit_cost, 4)\r\n    row.amount          = round(row_net_amount, 4)\r\n    row.net_rate        = round(net_unit_cost, 4)\r\n    row.net_amount      = round(row_net_amount, 4)\r\n    row.base_rate       = round(net_unit_cost, 4)\r\n    row.base_amount     = round(row_net_amount, 4)\r\n    row.base_net_rate   = round(net_unit_cost, 4)\r\n    row.base_net_amount = round(row_net_amount, 4)\r\n    row.valuation_rate  = round(net_unit_cost, 4)\r\n\r\n    # ── Accumulate ────────────────────────────────────────────\r\n    total_gross    += gross_total\r\n    total_discount += discount_value\r\n    total_trade    += trade_offer_total\r\n    total_net      += row_net_amount\r\n    total_gst      += gst_amount\r\n    total_adv_tax  += adv_tax_amount\r\n    total_fed      += fed_amount\r\n    total_line     += line_total\r\n\r\n\r\n# ── Round totals ──────────────────────────────────────────────\r\ntotal_gross    = round(total_gross, 4)\r\ntotal_discount = round(total_discount, 4)\r\ntotal_trade    = round(total_trade, 4)\r\ntotal_net      = round(total_net, 4)\r\ntotal_gst      = round(total_gst, 4)\r\ntotal_adv_tax  = round(total_adv_tax, 4)\r\ntotal_fed      = round(total_fed, 4)\r\ntotal_line     = round(total_line, 4)\r\ngrand_total    = round(total_net + total_gst + total_adv_tax, 4)\r\n\r\n# ── Custom summary fields ─────────────────────────────────────\r\ndoc.custom_total_gross       = total_gross\r\ndoc.custom_total_discount    = total_discount\r\ndoc.custom_total_trade_offer = total_trade\r\ndoc.custom_total_gst         = total_gst\r\ndoc.custom_total_advance_tax = total_adv_tax\r\ndoc.custom_total_fed         = total_fed\r\ndoc.custom_grand_line_total  = total_line\r\n\r\n# ── Core totals ───────────────────────────────────────────────\r\ndoc.total          = total_net\r\ndoc.net_total      = total_net\r\ndoc.base_total     = total_net\r\ndoc.base_net_total = total_net\r\n\r\n# ── Rebuild Taxes and Charges ─────────────────────────────────\r\ndoc.taxes = []\r\n\r\ncost_center = doc.cost_center\r\nif not cost_center:\r\n    cost_center = frappe.db.get_value('Company', doc.company, 'cost_center')\r\n\r\nif total_gst and gst_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : gst_account,\r\n        'description'                           : 'GST',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_gst,\r\n        'base_tax_amount'                       : total_gst,\r\n        'tax_amount_after_discount_amount'      : total_gst,\r\n        'base_tax_amount_after_discount_amount' : total_gst,\r\n        'total'                                 : round(total_net + total_gst, 4),\r\n        'base_total'                            : round(total_net + total_gst, 4),\r\n        'cost_center'                           : cost_center\r\n    })\r\n\r\nif total_adv_tax and adv_account:\r\n    doc.append('taxes', {\r\n        'charge_type'                           : 'Actual',\r\n        'account_head'                          : adv_account,\r\n        'description'                           : 'Advance Tax',\r\n        'category'                              : 'Total',\r\n        'add_deduct_tax'                        : 'Add',\r\n        'included_in_print_rate'                : 0,\r\n        'included_in_paid_amount'               : 0,\r\n        'tax_amount'                            : total_adv_tax,\r\n        'base_tax_amount'                       : total_adv_tax,\r\n        'tax_amount_after_discount_amount'      : total_adv_tax,\r\n        'base_tax_amount_after_discount_amount' : total_adv_tax,\r\n        'total'                                 : grand_total,\r\n        'base_total'                            : grand_total,\r\n        'cost_center'                           : cost_center\r\n    })\r\n\r\ndoc.grand_total      = grand_total\r\ndoc.base_grand_total = grand_total\r\ndoc.rounded_total    = round(grand_total, 0)\r\ndebug_info = []\r\nfor idx, row in enumerate(doc.items, 1):\r\n    debug_info.append(\r\n        f\"━━ ROW {idx}: {row.item_code} ━━<br>\"\r\n        f\"INPUTS:<br>\"\r\n        f\"&nbsp;&nbsp;rate={row.rate} | custom_vendor_rate={row.custom_vendor_rate}<br>\"\r\n        f\"&nbsp;&nbsp;qty={row.qty} | scheme_qty={row.custom_scheme_qty}<br>\"\r\n        f\"&nbsp;&nbsp;discount_per={row.custom_discount_per} | trade_offer={row.custom_trade_offer}<br>\"\r\n        f\"&nbsp;&nbsp;mrp={row.custom_mrp} | is_3rd={row.custom_is_3rd_schedule}<br>\"\r\n        f\"&nbsp;&nbsp;gst_per={row.custom_gst_per} | adv_tax_per={row.custom_advance_tax_per} | fed_per={row.custom_fed_per}<br>\"\r\n        f\"FORMULAS:<br>\"\r\n        f\"&nbsp;&nbsp;gst_formula={row.custom_gst_formula}<br>\"\r\n        f\"&nbsp;&nbsp;adv_formula={row.custom_advance_tax_formula}<br>\"\r\n        f\"&nbsp;&nbsp;fed_formula={row.custom_fed_formula}<br>\"\r\n        f\"CALCULATED ROW:<br>\"\r\n        f\"&nbsp;&nbsp;gross_total={row.custom_gross_total}<br>\"\r\n        f\"&nbsp;&nbsp;discount_amnt={row.custom_discount_amnt}<br>\"\r\n        f\"&nbsp;&nbsp;trade_offer_total={row.custom_trade_offer_total}<br>\"\r\n        f\"&nbsp;&nbsp;gst_amount={row.custom_gst_amount}<br>\"\r\n        f\"&nbsp;&nbsp;adv_tax_amount={row.custom_advance_tax_amount}<br>\"\r\n        f\"&nbsp;&nbsp;fed_amount={row.custom_fed_amount}<br>\"\r\n        f\"&nbsp;&nbsp;line_total={row.custom_line_total}<br>\"\r\n        f\"CORE FIELDS:<br>\"\r\n        f\"&nbsp;&nbsp;row.rate={row.rate} | row.amount={row.amount}<br>\"\r\n        f\"&nbsp;&nbsp;row.net_rate={row.net_rate} | row.net_amount={row.net_amount}<br>\"\r\n        f\"&nbsp;&nbsp;row.valuation_rate={row.valuation_rate}<br>\"\r\n    )\r\n\r\nfrappe.msgprint(\r\n    f\"<b>━━━━━ FULL DEBUG ━━━━━</b><br>\"\r\n    f\"{'<br>'.join(debug_info)}\"\r\n    f\"<br><b>━━ ACCUMULATED TOTALS ━━</b><br>\"\r\n    f\"total_gross={total_gross}<br>\"\r\n    f\"total_discount={total_discount}<br>\"\r\n    f\"total_trade={total_trade}<br>\"\r\n    f\"total_net={total_net}<br>\"\r\n    f\"total_gst={total_gst}<br>\"\r\n    f\"total_adv_tax={total_adv_tax}<br>\"\r\n    f\"total_fed={total_fed}<br>\"\r\n    f\"grand_total={grand_total}<br>\"\r\n    f\"<br><b>━━ DOC FIELDS ━━</b><br>\"\r\n    f\"doc.total={doc.total}<br>\"\r\n    f\"doc.net_total={doc.net_total}<br>\"\r\n    f\"doc.grand_total={doc.grand_total}<br>\"\r\n    f\"<br><b>━━ ACCOUNTS ━━</b><br>\"\r\n    f\"gst_account={gst_account}<br>\"\r\n    f\"adv_account={adv_account}<br>\"\r\n    f\"taxes_rows_added={len(doc.taxes)}<br>\",\r\n    title=\"Full Debug\",\r\n    indicator=\"blue\"\r\n)",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-06-15 06:45:31.698315",
  "module": "Aimatic",
  "name": "Validate",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Tax Formula",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Tax Formula validation\r\n# DocType : Tax Formula\r\n# Event   : Before Save\r\n# ============================================================\r\n\r\nALLOWED_VARS = {\r\n    'rate',\r\n    'qty',\r\n    'custom_scheme_qty',\r\n    'custom_discount_per',\r\n    'custom_trade_offer',\r\n    'custom_mrp',\r\n    'custom_gst_per',\r\n    'custom_advance_tax_per',\r\n    'custom_fed_per',\r\n    'custom_gross_total',\r\n    'custom_discount_amnt',\r\n    'custom_trade_offer_total',\r\n    'custom_gst_amount',\r\n    'custom_advance_tax_amount',\r\n    'custom_fed_amount',\r\n}\r\n\r\nALLOWED_CHARS = set('0123456789.+-*/() \\t\\n_')\r\nfor v in ALLOWED_VARS:\r\n    for ch in v:\r\n        ALLOWED_CHARS.add(ch)\r\n\r\nif doc.formula_string:\r\n    expr = doc.formula_string.strip()\r\n\r\n    # ── Extract identifiers (words starting with letter or _) ──\r\n    found = set()\r\n    word = ''\r\n    for ch in expr:\r\n        if ch.isalnum() or ch == '_':\r\n            word += ch\r\n        else:\r\n            if word and (word[0].isalpha() or word[0] == '_'):\r\n                found.add(word)\r\n            word = ''\r\n    if word and (word[0].isalpha() or word[0] == '_'):\r\n        found.add(word)\r\n\r\n    # ── Check invalid variables ──────────────────────────────\r\n    invalid = found - ALLOWED_VARS\r\n    if invalid:\r\n        allowed_list = ', '.join(sorted(ALLOWED_VARS))\r\n        invalid_list = ', '.join(sorted(invalid))\r\n        frappe.throw(\r\n            \"Invalid variable(s) in formula: <b>\" + invalid_list + \"</b><br><br>\"\r\n            \"<b>Allowed variables:</b><br>\" + allowed_list\r\n        )\r\n\r\n    # ── Balanced parentheses ─────────────────────────────────\r\n    if expr.count('(') != expr.count(')'):\r\n        frappe.throw(\"Unbalanced parentheses in formula string\")\r\n\r\n    # ── Character whitelist check ────────────────────────────\r\n    for ch in expr:\r\n        if ch not in ALLOWED_CHARS:\r\n            frappe.throw(\r\n                \"Invalid character in formula: <b>'\" + ch + \"'</b><br>\"\r\n                \"Only numbers, operators (+ - * /), parentheses, and allowed variable names are permitted.\"\r\n            )",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Validate",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-08 20:08:42.438636",
  "module": "Aimatic",
  "name": "pichatgpt",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Invoice",
  "script": "# ============================================================\r\n# PURCHASE INVOICE — CUSTOM COST/TAX ENGINE\r\n# DocType Event: Before Validate\r\n# ERPNext/Frappe v16\r\n#\r\n# IMPORTANT:\r\n# - This script prepares item rates and tax rows.\r\n# - ERPNext calculates document totals itself.\r\n# - Do not manually set grand_total/payment_schedule/base totals.\r\n# - On a Return: reuse the original invoice/receipt line's\r\n#   per-unit rate/price exactly, and scale its total fields\r\n#   (gross, discount, trade offer, FED, GST, advance tax) by\r\n#   the exact fraction of quantity being returned - never\r\n#   re-derive them from custom_scheme_qty/custom_trade_offer_total,\r\n#   which core does not scale on a return.\r\n# ============================================================\r\ndef evaluate_formula(formula_name, namespace):\r\n\r\n    if not formula_name:\r\n        return 0.0\r\n\r\n    try:\r\n\r\n        tf = frappe.get_doc(\"Tax Formula\", formula_name)\r\n\r\n        expr = (tf.formula_string or \"\").strip()\r\n\r\n        if not expr:\r\n            return 0.0\r\n\r\n        # replace variables\r\n        for k in sorted(namespace, key=len, reverse=True):\r\n\r\n            try:\r\n                val = float(namespace[k] or 0)\r\n            except:\r\n                val = 0\r\n\r\n            expr = expr.replace(k, str(val))\r\n\r\n        # remove spaces\r\n        expr = expr.replace(\" \", \"\")\r\n\r\n        # recursive calculator\r\n        def calc(e):\r\n\r\n            while \"(\" in e:\r\n                start = e.rfind(\"(\")\r\n                end = e.find(\")\", start)\r\n\r\n                inner = calc(e[start+1:end])\r\n\r\n                e = e[:start] + str(inner) + e[end+1:]\r\n\r\n            tokens = []\r\n            num = \"\"\r\n\r\n            for c in e:\r\n                if c in \"+-*/\":\r\n                    if num:\r\n                        tokens.append(float(num))\r\n                    tokens.append(c)\r\n                    num = \"\"\r\n                else:\r\n                    num += c\r\n\r\n            if num:\r\n                tokens.append(float(num))\r\n\r\n            i = 0\r\n            while i < len(tokens):\r\n                if tokens[i] == \"*\":\r\n                    tokens[i-1:i+2] = [tokens[i-1] * tokens[i+1]]\r\n                    i = 0\r\n                elif tokens[i] == \"/\":\r\n                    tokens[i-1:i+2] = [tokens[i-1] / tokens[i+1]]\r\n                    i = 0\r\n                else:\r\n                    i += 1\r\n\r\n            result = tokens[0]\r\n\r\n            i = 1\r\n\r\n            while i < len(tokens):\r\n                if tokens[i] == \"+\":\r\n                    result += tokens[i+1]\r\n\r\n                elif tokens[i] == \"-\":\r\n                    result -= tokens[i+1]\r\n\r\n                i += 2\r\n\r\n            return result\r\n\r\n        return round(float(calc(expr)), 4)\r\n\r\n    except Exception as e:\r\n\r\n        frappe.msgprint(\r\n            f\"\"\"\r\n            Formula: {formula_name}<br>\r\n            Expression: {expr}<br>\r\n            Error: {str(e)}\r\n            \"\"\",\r\n            title=\"Formula Error\",\r\n            indicator=\"red\"\r\n        )\r\n\r\n        return 0.0\r\n\r\n\r\n# ============================================================\r\n# RETURN BASIS\r\n#\r\n# Core's return mapper negates only qty/stock_qty/received_qty/\r\n# rejected_qty. custom_scheme_qty and custom_trade_offer_total\r\n# are copied verbatim and unscaled onto a return row, so re-\r\n# deriving a per-unit cost from them there is wrong. Instead,\r\n# for a return row, look up the original line - a debit note\r\n# billed directly against a submitted Purchase Invoice carries\r\n# the backlink in purchase_invoice_item; one built from a\r\n# Purchase Receipt return instead carries it in pr_detail - and\r\n# reuse its already-correct numbers: per-unit facts (rate, price\r\n# after taxes) are copied verbatim; whole-line totals (gross,\r\n# discount, trade offer, FED, GST, advance tax) are scaled by\r\n# the exact fraction of quantity being returned. This is exact\r\n# for both a full return and a partial return of a line that had\r\n# a scheme quantity, because row_net_amount_original =\r\n# rate_original * qty_original by construction, so scaling it by\r\n# the same fraction always reproduces rate_original *\r\n# qty_returned exactly.\r\n# ============================================================\r\n\r\ndef get_return_source_row(row):\r\n\r\n    # Field list is inlined here, not a module-level constant -\r\n    # Server Script sandbox binds nested def()s to a separate\r\n    # globals dict that does not include this script's own\r\n    # top-level names, so a helper function can only see its own\r\n    # parameters/locals, never a sibling top-level variable.\r\n    fields = [\r\n        \"qty\",\r\n        \"rate\",\r\n        \"custom_price_after_taxes\",\r\n        \"custom_gross_total\",\r\n        \"custom_discount_amnt\",\r\n        \"custom_trade_offer_total\",\r\n        \"custom_fed_amount\",\r\n        \"custom_gst_amount\",\r\n        \"custom_advance_tax_amount\"\r\n    ]\r\n\r\n    source_name = row.get(\"purchase_invoice_item\")\r\n\r\n    if source_name:\r\n\r\n        source = frappe.db.get_value(\r\n            \"Purchase Invoice Item\",\r\n            source_name,\r\n            fields,\r\n            as_dict=True\r\n        )\r\n\r\n        if source:\r\n            return source\r\n\r\n    source_name = row.get(\"pr_detail\")\r\n\r\n    if source_name:\r\n\r\n        return frappe.db.get_value(\r\n            \"Purchase Receipt Item\",\r\n            source_name,\r\n            fields,\r\n            as_dict=True\r\n        )\r\n\r\n    return None\r\n\r\n\r\ntotal_gross = 0.0\r\ntotal_discount = 0.0\r\ntotal_trade = 0.0\r\ntotal_fed = 0.0\r\ntotal_gst = 0.0\r\ntotal_adv_tax = 0.0\r\ntotal_net = 0.0\r\ntotal_line = 0.0\r\n\r\ngst_account = None\r\nadv_account = None\r\n\r\n\r\n# Prevent Pricing Rules from applying another discount\r\ndoc.ignore_pricing_rule = 1\r\n\r\n\r\nis_return = int(doc.is_return or 0)\r\n\r\n\r\nfor row in doc.items:\r\n\r\n    # --------------------------------------------------------\r\n    # INPUTS\r\n    # --------------------------------------------------------\r\n\r\n    vendor_rate = float(row.custom_vendor_rate or 0)\r\n\r\n    # First creation fallback only\r\n    if not vendor_rate:\r\n        vendor_rate = float(row.price_list_rate or row.rate or 0)\r\n\r\n    qty = float(row.qty or 0)\r\n    scheme_qty = float(row.custom_scheme_qty or 0)\r\n    discount_per = float(row.custom_discount_per or 0)\r\n    trade_offer_total = float(row.custom_trade_offer_total or 0)\r\n    mrp = float(row.custom_mrp or 0)\r\n\r\n    gst_rate = float(row.custom_gst_per or 0)\r\n    adv_tax_rate = float(row.custom_advance_tax_per or 0)\r\n    fed_rate = float(row.custom_fed_per or 0)\r\n\r\n    is_3rd_schedule = int(row.custom_is_3rd_schedule or 0)\r\n\r\n    # A return does not carry a meaningful scheme quantity of its\r\n    # own - zero it rather than trust the raw carryover.\r\n    if is_return:\r\n        scheme_qty = 0\r\n\r\n    if scheme_qty < 0:\r\n        scheme_qty = 0\r\n\r\n    if scheme_qty > qty and not is_return:\r\n        frappe.throw(\r\n            \"Scheme quantity cannot exceed total quantity for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    paid_qty = qty - scheme_qty\r\n\r\n    # --------------------------------------------------------\r\n    # FORMULA NAMES\r\n    # --------------------------------------------------------\r\n\r\n    gst_formula_name = row.custom_gst_formula or \"\"\r\n    adv_formula_name = row.custom_advance_tax_formula or \"\"\r\n    fed_formula_name = row.custom_fed_formula or \"\"\r\n\r\n    # 3rd Schedule formula override\r\n    if is_3rd_schedule:\r\n\r\n        if (\r\n            not gst_formula_name\r\n            or not gst_formula_name.lower().startswith(\"3rd\")\r\n        ):\r\n            matching_formula = frappe.db.get_value(\r\n                \"Tax Formula\",\r\n                {\r\n                    \"formula_name\": [\"like\", \"%3rd Schedule%\"],\r\n                    \"formula_type\": \"gst\"\r\n                },\r\n                \"name\"\r\n            )\r\n\r\n            if matching_formula:\r\n                gst_formula_name = matching_formula\r\n                row.custom_gst_formula = matching_formula\r\n\r\n    # --------------------------------------------------------\r\n    # ACCOUNTS\r\n    # --------------------------------------------------------\r\n\r\n    if gst_formula_name and not gst_account:\r\n        gst_account = frappe.db.get_value(\r\n            \"Tax Formula\",\r\n            gst_formula_name,\r\n            \"gst_account\"\r\n        )\r\n\r\n    if adv_formula_name and not adv_account:\r\n        adv_account = frappe.db.get_value(\r\n            \"Tax Formula\",\r\n            adv_formula_name,\r\n            \"advance_tax_account\"\r\n        )\r\n\r\n    # Document-level fallbacks\r\n    if not gst_account:\r\n        gst_account = doc.custom_gst_input_account\r\n\r\n    if not adv_account:\r\n        adv_account = doc.custom_advance_tax_account\r\n\r\n    # --------------------------------------------------------\r\n    # RETURN BASIS LOOKUP\r\n    # --------------------------------------------------------\r\n\r\n    return_source = get_return_source_row(row) if is_return else None\r\n\r\n    return_base_qty = float(return_source.get(\"qty\") or 0) if return_source else 0.0\r\n\r\n    use_return_basis = bool(return_source) and return_base_qty != 0\r\n\r\n    # ==========================================================\r\n    # RETURN PATH - reuse the original line's numbers exactly\r\n    # ==========================================================\r\n\r\n    if use_return_basis:\r\n\r\n        return_ratio = qty / return_base_qty\r\n\r\n        gross_total = round(\r\n            float(return_source.get(\"custom_gross_total\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        discount_amount = round(\r\n            float(return_source.get(\"custom_discount_amnt\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        trade_offer_total = round(\r\n            float(return_source.get(\"custom_trade_offer_total\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        trade_offer_per_unit = round(\r\n            float(row.custom_trade_offer or 0),\r\n            4\r\n        )\r\n\r\n        fed_amount = round(\r\n            float(return_source.get(\"custom_fed_amount\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        gst_amount = round(\r\n            float(return_source.get(\"custom_gst_amount\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        adv_tax_amount = round(\r\n            float(return_source.get(\"custom_advance_tax_amount\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        row_net_amount = round(\r\n            gross_total\r\n            - discount_amount\r\n            - trade_offer_total\r\n            + fed_amount,\r\n            4\r\n        )\r\n\r\n        # Verbatim - the same per-unit cost the goods were\r\n        # billed at, never re-derived through the breakdown above.\r\n        net_unit_cost = round(\r\n            float(return_source.get(\"rate\") or 0),\r\n            4\r\n        )\r\n\r\n        line_total = round(\r\n            row_net_amount\r\n            + gst_amount\r\n            + adv_tax_amount,\r\n            4\r\n        )\r\n\r\n        price_after_taxes = round(\r\n            float(return_source.get(\"custom_price_after_taxes\") or 0),\r\n            4\r\n        )\r\n\r\n    # ==========================================================\r\n    # FORWARD PATH (also used as the return fallback below when\r\n    # a return row has no traceable original line)\r\n    # ==========================================================\r\n\r\n    else:\r\n\r\n        if is_return and trade_offer_total:\r\n\r\n            # No traceable original line to scale from - the\r\n            # carried-over lump-sum Trade Offer Total belongs to\r\n            # the ORIGINAL quantity, not this return's own. Fall\r\n            # back to whatever per-unit Trade Offer is already on\r\n            # this row, if any, otherwise zero it and warn.\r\n\r\n            per_unit_trade_offer = float(row.custom_trade_offer or 0)\r\n\r\n            if per_unit_trade_offer:\r\n\r\n                trade_offer_total = round(\r\n                    per_unit_trade_offer * qty,\r\n                    4\r\n                )\r\n\r\n                frappe.msgprint(\r\n                    \"Return row for item \"\r\n                    + str(row.item_code)\r\n                    + \" has no traceable original invoice/receipt line - \"\r\n                    \"Trade Offer Total was estimated from the per-unit \"\r\n                    \"Trade Offer already on this row. Verify this amount \"\r\n                    \"before submitting.\",\r\n                    title=\"Return Trade Offer Estimated\",\r\n                    indicator=\"orange\"\r\n                )\r\n\r\n            else:\r\n\r\n                frappe.msgprint(\r\n                    \"Return row for item \"\r\n                    + str(row.item_code)\r\n                    + \" has no traceable original invoice/receipt line \"\r\n                    \"and no per-unit Trade Offer to estimate from - \"\r\n                    \"Trade Offer Total has been set to 0. Enter it \"\r\n                    \"manually if this return should carry one.\",\r\n                    title=\"Return Trade Offer Skipped\",\r\n                    indicator=\"orange\"\r\n                )\r\n\r\n                trade_offer_total = 0.0\r\n\r\n        # ----------------------------------------------------\r\n        # COMMERCIAL VALUES\r\n        # ----------------------------------------------------\r\n\r\n        gross_total = vendor_rate * qty\r\n\r\n        discount_amount = (\r\n            vendor_rate\r\n            * discount_per\r\n            / 100.0\r\n            * paid_qty\r\n        )\r\n\r\n        trade_offer_per_unit = trade_offer_total / paid_qty if paid_qty else 0\r\n\r\n        # ----------------------------------------------------\r\n        # FORMULA NAMESPACE\r\n        # ----------------------------------------------------\r\n\r\n        ns = {\r\n            \"rate\": vendor_rate,\r\n            \"qty\": qty,\r\n            \"custom_scheme_qty\": scheme_qty,\r\n            \"custom_discount_per\": discount_per,\r\n            \"custom_trade_offer\": trade_offer_per_unit,\r\n            \"custom_mrp\": mrp,\r\n            \"custom_gst_per\": gst_rate,\r\n            \"custom_advance_tax_per\": adv_tax_rate,\r\n            \"custom_fed_per\": fed_rate,\r\n            \"custom_gross_total\": gross_total,\r\n            \"custom_discount_amnt\": discount_amount,\r\n            \"custom_trade_offer_total\": trade_offer_total,\r\n            \"custom_gst_amount\": 0.0,\r\n            \"custom_advance_tax_amount\": 0.0,\r\n            \"custom_fed_amount\": 0.0\r\n        }\r\n\r\n        # ----------------------------------------------------\r\n        # FED\r\n        # ----------------------------------------------------\r\n\r\n        if fed_formula_name:\r\n            fed_amount = evaluate_formula(fed_formula_name, ns)\r\n\r\n        elif fed_rate:\r\n            fed_amount = (\r\n                vendor_rate\r\n                * paid_qty\r\n                * fed_rate\r\n                / 100.0\r\n            )\r\n\r\n        else:\r\n            fed_amount = 0.0\r\n\r\n        ns[\"custom_fed_amount\"] = fed_amount\r\n\r\n        # ----------------------------------------------------\r\n        # GST\r\n        # ----------------------------------------------------\r\n\r\n        if gst_formula_name:\r\n            gst_amount = evaluate_formula(gst_formula_name, ns)\r\n        else:\r\n            gst_amount = 0.0\r\n\r\n        ns[\"custom_gst_amount\"] = gst_amount\r\n\r\n        # ----------------------------------------------------\r\n        # ADVANCE TAX\r\n        # ----------------------------------------------------\r\n\r\n        if adv_formula_name:\r\n            adv_tax_amount = evaluate_formula(\r\n                adv_formula_name,\r\n                ns\r\n            )\r\n        else:\r\n            adv_tax_amount = 0.0\r\n\r\n        ns[\"custom_advance_tax_amount\"] = adv_tax_amount\r\n\r\n        # ----------------------------------------------------\r\n        # INVENTORY / EXPENSE COST\r\n        # ----------------------------------------------------\r\n\r\n        row_net_amount = (\r\n            gross_total\r\n            - discount_amount\r\n            - trade_offer_total\r\n            + fed_amount\r\n        )\r\n\r\n        if qty:\r\n            net_unit_cost = row_net_amount / qty\r\n        else:\r\n            net_unit_cost = 0.0\r\n\r\n        line_total = (\r\n            row_net_amount\r\n            + gst_amount\r\n            + adv_tax_amount\r\n        )\r\n\r\n        price_after_taxes = round(\r\n            line_total / qty if qty else 0,\r\n            4\r\n        )\r\n\r\n    # --------------------------------------------------------\r\n    # CUSTOM DISPLAY FIELDS\r\n    # --------------------------------------------------------\r\n\r\n    row.custom_vendor_rate = round(vendor_rate, 4)\r\n    row.custom_scheme_qty = round(scheme_qty, 4)\r\n    row.custom_gross_total = round(gross_total, 4)\r\n    row.custom_discount_amnt = round(discount_amount, 4)\r\n    row.custom_trade_offer_total = round(\r\n        trade_offer_total,\r\n        4\r\n    )\r\n    row.custom_trade_offer = round(trade_offer_per_unit, 4)\r\n\r\n    row.custom_fed_amount = round(fed_amount, 4)\r\n    row.custom_gst_amount = round(gst_amount, 4)\r\n    row.custom_advance_tax_amount = round(\r\n        adv_tax_amount,\r\n        4\r\n    )\r\n\r\n    row.custom_price_without_taxes = round(\r\n        net_unit_cost,\r\n        4\r\n    )\r\n\r\n    row.custom_price_after_taxes = price_after_taxes\r\n\r\n    row.custom_line_total = round(line_total, 4)\r\n\r\n    # --------------------------------------------------------\r\n    # STANDARD ERPNext ITEM FIELDS\r\n    #\r\n    # Use only the final inventory/expense rate here.\r\n    # ERPNext calculates amount/net/base fields.\r\n    # --------------------------------------------------------\r\n\r\n    row.price_list_rate = round(vendor_rate, 4)\r\n    row.base_price_list_rate = round(vendor_rate, 4)\r\n\r\n    row.rate = round(net_unit_cost, 4)\r\n\r\n    # Clear standard discount fields because custom discount is\r\n    # already included in net_unit_cost.\r\n    row.discount_percentage = 0\r\n    row.discount_amount = 0\r\n    row.distributed_discount_amount = 0\r\n\r\n    # Do not assign:\r\n    # row.amount\r\n    # row.net_rate\r\n    # row.net_amount\r\n    # row.base_rate\r\n    # row.base_amount\r\n    # row.valuation_rate\r\n\r\n    # --------------------------------------------------------\r\n    # TOTALS FOR CUSTOM SUMMARY/TAX ROWS\r\n    # --------------------------------------------------------\r\n\r\n    total_gross += gross_total\r\n    total_discount += discount_amount\r\n    total_trade += trade_offer_total\r\n    total_fed += fed_amount\r\n    total_gst += gst_amount\r\n    total_adv_tax += adv_tax_amount\r\n    total_net += row_net_amount\r\n    total_line += line_total\r\n\r\n\r\n# ============================================================\r\n# ROUND CUSTOM TOTALS\r\n# ============================================================\r\n\r\ntotal_gross = round(total_gross, 4)\r\ntotal_discount = round(total_discount, 4)\r\ntotal_trade = round(total_trade, 4)\r\ntotal_fed = round(total_fed, 4)\r\ntotal_gst = round(total_gst, 4)\r\ntotal_adv_tax = round(total_adv_tax, 4)\r\ntotal_net = round(total_net, 4)\r\ntotal_line = round(total_line, 4)\r\n\r\n\r\n# ============================================================\r\n# VALIDATE TAX ACCOUNTS\r\n# ============================================================\r\n\r\nif total_gst and not gst_account:\r\n    frappe.throw(\r\n        \"GST amount was calculated, but the GST account is missing.\"\r\n    )\r\n\r\nif total_adv_tax and not adv_account:\r\n    frappe.throw(\r\n        \"Advance Tax was calculated, but the Advance Tax account \"\r\n        \"is missing.\"\r\n    )\r\n\r\n\r\n# ============================================================\r\n# CUSTOM DOCUMENT SUMMARY\r\n# ============================================================\r\n\r\ndoc.custom_total_gross = total_gross\r\ndoc.custom_total_discount = total_discount\r\ndoc.custom_total_trade_offer = total_trade\r\ndoc.custom_total_fed = total_fed\r\ndoc.custom_total_gst = total_gst\r\ndoc.custom_total_advance_tax = total_adv_tax\r\ndoc.custom_grand_line_total = total_line\r\n\r\n\r\n# ============================================================\r\n# REBUILD PURCHASE TAXES AND CHARGES\r\n#\r\n# ERPNext will calculate:\r\n# total_taxes_and_charges\r\n# grand_total\r\n# rounded_total\r\n# payment_schedule\r\n# GL values\r\n# ============================================================\r\n\r\ndoc.set(\"taxes\", [])\r\n\r\n\r\ncost_center = doc.cost_center\r\n\r\nif not cost_center:\r\n    cost_center = frappe.db.get_value(\r\n        \"Company\",\r\n        doc.company,\r\n        \"cost_center\"\r\n    )\r\n\r\n\r\nif total_gst:\r\n\r\n    gst_row = doc.append(\"taxes\", {})\r\n\r\n    gst_row.charge_type = \"Actual\"\r\n    gst_row.account_head = gst_account\r\n    gst_row.description = \"GST\"\r\n    gst_row.category = \"Total\"\r\n    gst_row.add_deduct_tax = \"Add\"\r\n    gst_row.included_in_print_rate = 0\r\n    gst_row.included_in_paid_amount = 0\r\n    gst_row.tax_amount = total_gst\r\n    gst_row.cost_center = cost_center\r\n\r\n\r\nif total_adv_tax:\r\n\r\n    advance_row = doc.append(\"taxes\", {})\r\n\r\n    advance_row.charge_type = \"Actual\"\r\n    advance_row.account_head = adv_account\r\n    advance_row.description = \"Advance Tax\"\r\n    advance_row.category = \"Total\"\r\n    advance_row.add_deduct_tax = \"Add\"\r\n    advance_row.included_in_print_rate = 0\r\n    advance_row.included_in_paid_amount = 0\r\n    advance_row.tax_amount = total_adv_tax\r\n    advance_row.cost_center = cost_center\r\n\r\n\r\n# DO NOT manually assign any of the following:\r\n#\r\n# doc.total\r\n# doc.net_total\r\n# doc.base_total\r\n# doc.base_net_total\r\n# doc.taxes_and_charges_added\r\n# doc.total_taxes_and_charges\r\n# doc.grand_total\r\n# doc.base_grand_total\r\n# doc.rounding_adjustment\r\n# doc.rounded_total\r\n# doc.outstanding_amount\r\n# doc.payment_schedule",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Validate",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-08 20:07:53.178559",
  "module": "Aimatic",
  "name": "PRV1",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Receipt",
  "script": "# ============================================================\r\n# SERVER SCRIPT — PURCHASE RECEIPT — DYNAMIC COST ENGINE\r\n#\r\n# DocType : Purchase Receipt\r\n# Event   : Before Validate\r\n# ERPNext : v16\r\n#\r\n# PURPOSE\r\n# ------------------------------------------------------------\r\n# 1. Preserve vendor price in custom_vendor_rate\r\n# 2. Calculate:\r\n#       Gross\r\n#       Discount\r\n#       Trade Offer\r\n#       FED\r\n#       GST preview\r\n#       Advance Tax preview\r\n# 3. Inventory Cost:\r\n#       Gross - Discount - Trade Offer + FED\r\n# 4. Set ERPNext item rate to net inventory cost per unit\r\n# 5. Do not create GST/Advance Tax accounting rows on PR\r\n# 6. Let ERPNext calculate document totals\r\n# 7. On a Return: reuse the original receipt line's per-unit\r\n#    rate/price exactly, and scale its total fields (gross,\r\n#    discount, trade offer, FED, GST, advance tax) by the exact\r\n#    fraction of quantity being returned - never re-derive them\r\n#    from custom_scheme_qty/custom_trade_offer_total, which\r\n#    core does not scale on a return and which do not carry a\r\n#    usable per-unit meaning for a partial return.\r\n#\r\n# FORMULA OPERATORS SUPPORTED:\r\n#       +  -  *  /  ( )\r\n# ============================================================\r\n\r\n\r\n# ============================================================\r\n# SETTINGS\r\n# ============================================================\r\n\r\nSHOW_DEBUG = 0\r\n\r\n\r\n# ============================================================\r\n# FORMULA ENGINE\r\n#\r\n# Server Script sandbox does not allow:\r\n# - eval()\r\n# - frappe.safe_eval()\r\n# - frappe.utils.safe_exec()\r\n#\r\n# This parser supports:\r\n# - addition\r\n# - subtraction\r\n# - multiplication\r\n# - division\r\n# - parentheses\r\n# - positive/negative numeric values\r\n# ============================================================\r\n\r\ndef evaluate_formula(formula_name, namespace):\r\n\r\n    if not formula_name:\r\n        return 0.0\r\n\r\n    expr = \"\"\r\n\r\n    try:\r\n        formula_doc = frappe.get_doc(\r\n            \"Tax Formula\",\r\n            formula_name\r\n        )\r\n\r\n        expr = (formula_doc.formula_string or \"\").strip()\r\n\r\n        if not expr:\r\n            return 0.0\r\n\r\n        # ----------------------------------------------------\r\n        # Replace formula variables with numeric values\r\n        # Longest variable names first\r\n        # ----------------------------------------------------\r\n\r\n        variable_names = sorted(\r\n            namespace,\r\n            key=len,\r\n            reverse=True\r\n        )\r\n\r\n        for variable_name in variable_names:\r\n\r\n            try:\r\n                variable_value = float(\r\n                    namespace.get(variable_name) or 0\r\n                )\r\n            except:\r\n                variable_value = 0.0\r\n\r\n            expr = expr.replace(\r\n                variable_name,\r\n                str(variable_value)\r\n            )\r\n\r\n        expr = expr.replace(\" \", \"\")\r\n\r\n        if not expr:\r\n            return 0.0\r\n\r\n        # ----------------------------------------------------\r\n        # Allow only arithmetic characters after substitution\r\n        # ----------------------------------------------------\r\n\r\n        allowed_characters = \"0123456789.+-*/()\"\r\n\r\n        for character in expr:\r\n            if character not in allowed_characters:\r\n                frappe.throw(\r\n                    \"Unsupported character '\"\r\n                    + str(character)\r\n                    + \"' in formula \"\r\n                    + str(formula_name)\r\n                )\r\n\r\n        # ----------------------------------------------------\r\n        # Recursive arithmetic parser\r\n        # ----------------------------------------------------\r\n\r\n        position = [0]\r\n        expression_length = len(expr)\r\n\r\n        def current_character():\r\n\r\n            if position[0] >= expression_length:\r\n                return \"\"\r\n\r\n            return expr[position[0]]\r\n\r\n        def consume_character():\r\n\r\n            character = current_character()\r\n            position[0] = position[0] + 1\r\n            return character\r\n\r\n        def parse_number():\r\n\r\n            number_text = \"\"\r\n\r\n            # Unary plus/minus\r\n            if current_character() in [\"+\", \"-\"]:\r\n                number_text += consume_character()\r\n\r\n            decimal_count = 0\r\n\r\n            while position[0] < expression_length:\r\n\r\n                character = current_character()\r\n\r\n                if character == \".\":\r\n                    decimal_count = decimal_count + 1\r\n\r\n                    if decimal_count > 1:\r\n                        frappe.throw(\r\n                            \"Invalid number in formula \"\r\n                            + str(formula_name)\r\n                        )\r\n\r\n                    number_text += consume_character()\r\n\r\n                elif character >= \"0\" and character <= \"9\":\r\n                    number_text += consume_character()\r\n\r\n                else:\r\n                    break\r\n\r\n            if (\r\n                not number_text\r\n                or number_text == \"+\"\r\n                or number_text == \"-\"\r\n                or number_text == \".\"\r\n                or number_text == \"+.\"\r\n                or number_text == \"-.\"\r\n            ):\r\n                frappe.throw(\r\n                    \"Invalid number near position \"\r\n                    + str(position[0])\r\n                    + \" in formula \"\r\n                    + str(formula_name)\r\n                )\r\n\r\n            return float(number_text)\r\n\r\n        def parse_factor():\r\n\r\n            character = current_character()\r\n\r\n            # Handle unary operators before parentheses/numbers\r\n            if character == \"+\":\r\n\r\n                consume_character()\r\n                return parse_factor()\r\n\r\n            if character == \"-\":\r\n\r\n                consume_character()\r\n                return -parse_factor()\r\n\r\n            if character == \"(\":\r\n\r\n                consume_character()\r\n\r\n                value = parse_expression()\r\n\r\n                if current_character() != \")\":\r\n                    frappe.throw(\r\n                        \"Missing closing parenthesis in formula \"\r\n                        + str(formula_name)\r\n                    )\r\n\r\n                consume_character()\r\n\r\n                return value\r\n\r\n            return parse_number()\r\n\r\n        def parse_term():\r\n\r\n            value = parse_factor()\r\n\r\n            while current_character() in [\"*\", \"/\"]:\r\n\r\n                operator = consume_character()\r\n                right_value = parse_factor()\r\n\r\n                if operator == \"*\":\r\n                    value = value * right_value\r\n\r\n                else:\r\n                    if right_value == 0:\r\n                        frappe.throw(\r\n                            \"Division by zero in formula \"\r\n                            + str(formula_name)\r\n                        )\r\n\r\n                    value = value / right_value\r\n\r\n            return value\r\n\r\n        def parse_expression():\r\n\r\n            value = parse_term()\r\n\r\n            while current_character() in [\"+\", \"-\"]:\r\n\r\n                operator = consume_character()\r\n                right_value = parse_term()\r\n\r\n                if operator == \"+\":\r\n                    value = value + right_value\r\n                else:\r\n                    value = value - right_value\r\n\r\n            return value\r\n\r\n        result = parse_expression()\r\n\r\n        if position[0] != expression_length:\r\n            frappe.throw(\r\n                \"Unexpected character near position \"\r\n                + str(position[0])\r\n                + \" in formula \"\r\n                + str(formula_name)\r\n            )\r\n\r\n        return round(float(result or 0), 4)\r\n\r\n    except Exception as error:\r\n\r\n        frappe.msgprint(\r\n            (\r\n                \"<b>Purchase Receipt Formula Error</b><br><br>\"\r\n                \"<b>Formula:</b> \"\r\n                + str(formula_name)\r\n                + \"<br>\"\r\n                \"<b>Expression:</b> \"\r\n                + str(expr)\r\n                + \"<br>\"\r\n                \"<b>Error:</b> \"\r\n                + str(error)\r\n            ),\r\n            title=\"Formula Error\",\r\n            indicator=\"red\"\r\n        )\r\n\r\n        return 0.0\r\n\r\n\r\n# ============================================================\r\n# RETURN BASIS\r\n#\r\n# Core's return mapper negates only qty/stock_qty/received_qty/\r\n# rejected_qty. custom_scheme_qty and custom_trade_offer_total\r\n# are copied verbatim and unscaled onto a return row, so re-\r\n# deriving a per-unit cost from them there is wrong - it mixes\r\n# a total meant for the ORIGINAL quantity with the RETURN's own\r\n# much smaller quantity. Instead, for a return row, look up the\r\n# original receipt line via its backlink and reuse its already-\r\n# correct numbers: per-unit facts (rate, price after taxes) are\r\n# copied verbatim; whole-line totals (gross, discount, trade\r\n# offer, FED, GST, advance tax) are scaled by the exact fraction\r\n# of quantity being returned. This is exact for both a full\r\n# return (fraction = -1, everything is negated exactly) and a\r\n# partial return of a line that had a scheme quantity, because\r\n# inventory_amount_original = rate_original * qty_original by\r\n# construction, so scaling it by the same fraction always\r\n# reproduces rate_original * qty_returned exactly - there is no\r\n# residual gap to approximate.\r\n# ============================================================\r\n\r\ndef get_return_source_row(row):\r\n\r\n    source_name = row.get(\"purchase_receipt_item\")\r\n\r\n    if not source_name:\r\n        return None\r\n\r\n    # Field list is inlined here, not a module-level constant -\r\n    # Server Script sandbox binds nested def()s to a separate\r\n    # globals dict that does not include this script's own\r\n    # top-level names, so a helper function can only see its own\r\n    # parameters/locals, never a sibling top-level variable.\r\n    return frappe.db.get_value(\r\n        \"Purchase Receipt Item\",\r\n        source_name,\r\n        [\r\n            \"qty\",\r\n            \"rejected_qty\",\r\n            \"rate\",\r\n            \"custom_price_after_taxes\",\r\n            \"custom_gross_total\",\r\n            \"custom_discount_amnt\",\r\n            \"custom_trade_offer_total\",\r\n            \"custom_fed_amount\",\r\n            \"custom_gst_amount\",\r\n            \"custom_advance_tax_amount\"\r\n        ],\r\n        as_dict=True\r\n    )\r\n\r\n\r\n# ============================================================\r\n# TOTAL VARIABLES\r\n# ============================================================\r\n\r\ntotal_gross = 0.0\r\ntotal_discount = 0.0\r\ntotal_trade_offer = 0.0\r\ntotal_fed = 0.0\r\ntotal_gst = 0.0\r\ntotal_advance_tax = 0.0\r\ntotal_inventory_cost = 0.0\r\ntotal_line_value = 0.0\r\n\r\n\r\n# Prevent ERPNext Pricing Rules from applying another discount\r\ndoc.ignore_pricing_rule = 1\r\n\r\n\r\nis_return = int(doc.is_return or 0)\r\n\r\n\r\n# ============================================================\r\n# PROCESS PURCHASE RECEIPT ITEMS\r\n# ============================================================\r\n\r\nfor row in doc.items:\r\n\r\n    # --------------------------------------------------------\r\n    # READ INPUTS\r\n    # --------------------------------------------------------\r\n\r\n    vendor_rate = float(\r\n        row.custom_vendor_rate or 0\r\n    )\r\n\r\n    # Initial fallback only\r\n    if not vendor_rate:\r\n\r\n        vendor_rate = float(\r\n            row.price_list_rate\r\n            or row.rate\r\n            or 0\r\n        )\r\n\r\n    qty = float(row.qty or 0)\r\n\r\n    scheme_qty = float(\r\n        row.custom_scheme_qty or 0\r\n    )\r\n\r\n    # A return does not carry a meaningful scheme quantity of\r\n    # its own - the original line's scheme units aren't being\r\n    # separately tracked as returned or not, so this is always\r\n    # zeroed on a return rather than trusting the raw carryover.\r\n    if is_return:\r\n        scheme_qty = 0.0\r\n\r\n    discount_per = float(\r\n        row.custom_discount_per or 0\r\n    )\r\n\r\n    trade_offer_total = float(\r\n        row.custom_trade_offer_total or 0\r\n    )\r\n\r\n    mrp = float(\r\n        row.custom_mrp or 0\r\n    )\r\n\r\n    gst_per = float(\r\n        row.custom_gst_per or 0\r\n    )\r\n\r\n    advance_tax_per = float(\r\n        row.custom_advance_tax_per or 0\r\n    )\r\n\r\n    fed_per = float(\r\n        row.custom_fed_per or 0\r\n    )\r\n\r\n    is_third_schedule = int(\r\n        row.custom_is_3rd_schedule or 0\r\n    )\r\n\r\n    # --------------------------------------------------------\r\n    # VALIDATIONS\r\n    # --------------------------------------------------------\r\n\r\n    if qty < 0 and not is_return:\r\n        frappe.throw(\r\n            \"Quantity cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if scheme_qty < 0 and not is_return:\r\n        frappe.throw(\r\n            \"Scheme quantity cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if scheme_qty > qty and not is_return:\r\n        frappe.throw(\r\n            \"Scheme quantity cannot exceed total quantity for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if discount_per < 0:\r\n        frappe.throw(\r\n            \"Discount percentage cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if discount_per > 100:\r\n        frappe.throw(\r\n            \"Discount percentage cannot exceed 100 for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if vendor_rate < 0:\r\n        frappe.throw(\r\n            \"Vendor rate cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    # --------------------------------------------------------\r\n    # RETURN BASIS LOOKUP\r\n    # --------------------------------------------------------\r\n\r\n    return_source = get_return_source_row(row) if is_return else None\r\n\r\n    return_base_qty = 0.0\r\n\r\n    if return_source:\r\n\r\n        if row.get(\"return_qty_from_rejected_warehouse\"):\r\n            return_base_qty = float(\r\n                return_source.get(\"rejected_qty\") or 0\r\n            )\r\n        else:\r\n            return_base_qty = float(\r\n                return_source.get(\"qty\") or 0\r\n            )\r\n\r\n    use_return_basis = bool(return_source) and return_base_qty != 0\r\n\r\n    # ==========================================================\r\n    # RETURN PATH - reuse the original line's numbers exactly\r\n    # ==========================================================\r\n\r\n    if use_return_basis:\r\n\r\n        return_ratio = qty / return_base_qty\r\n\r\n        gross_total = round(\r\n            float(return_source.get(\"custom_gross_total\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        discount_amount = round(\r\n            float(return_source.get(\"custom_discount_amnt\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        trade_offer_total = round(\r\n            float(return_source.get(\"custom_trade_offer_total\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        # Per-unit trade offer is a display figure only on a\r\n        # return - reuse whatever already carried over onto this\r\n        # row rather than re-dividing a rescaled total by this\r\n        # return's own (much smaller) paid quantity.\r\n        trade_offer_per_unit = round(\r\n            float(row.custom_trade_offer or 0),\r\n            4\r\n        )\r\n\r\n        fed_amount = round(\r\n            float(return_source.get(\"custom_fed_amount\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        gst_amount = round(\r\n            float(return_source.get(\"custom_gst_amount\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        advance_tax_amount = round(\r\n            float(return_source.get(\"custom_advance_tax_amount\") or 0)\r\n            * return_ratio,\r\n            4\r\n        )\r\n\r\n        inventory_amount = round(\r\n            gross_total\r\n            - discount_amount\r\n            - trade_offer_total\r\n            + fed_amount,\r\n            4\r\n        )\r\n\r\n        # Verbatim - the same per-unit cost the goods were\r\n        # received at, never re-derived through the breakdown\r\n        # above (that reconstruction is provably exact here, but\r\n        # verbatim avoids any rounding drift from intermediate\r\n        # 4-decimal roundings and matches the vendor's own\r\n        # expectation of \"same rate, negative quantity\").\r\n        net_unit_cost = round(\r\n            float(return_source.get(\"rate\") or 0),\r\n            4\r\n        )\r\n\r\n        line_total = round(\r\n            inventory_amount\r\n            + gst_amount\r\n            + advance_tax_amount,\r\n            4\r\n        )\r\n\r\n        price_after_taxes = round(\r\n            float(return_source.get(\"custom_price_after_taxes\") or 0),\r\n            4\r\n        )\r\n\r\n    # ==========================================================\r\n    # FORWARD PATH (also used as the return fallback below when\r\n    # a return row has no traceable original line)\r\n    # ==========================================================\r\n\r\n    else:\r\n\r\n        if is_return and trade_offer_total:\r\n\r\n            # No traceable original line to scale from. The\r\n            # carried-over lump-sum Trade Offer Total belongs to\r\n            # the ORIGINAL quantity, not this return's own much\r\n            # smaller quantity, so trusting it here would be the\r\n            # same class of bug this whole change fixes. Fall\r\n            # back to whatever per-unit Trade Offer already sits\r\n            # on this row, if any - otherwise zero it and warn,\r\n            # rather than silently apply the full original total.\r\n\r\n            per_unit_trade_offer = float(\r\n                row.custom_trade_offer or 0\r\n            )\r\n\r\n            if per_unit_trade_offer:\r\n\r\n                trade_offer_total = round(\r\n                    per_unit_trade_offer * qty,\r\n                    4\r\n                )\r\n\r\n                frappe.msgprint(\r\n                    \"Return row for item \"\r\n                    + str(row.item_code)\r\n                    + \" has no traceable original receipt line - \"\r\n                    \"Trade Offer Total was estimated from the \"\r\n                    \"per-unit Trade Offer already on this row. \"\r\n                    \"Verify this amount before submitting.\",\r\n                    title=\"Return Trade Offer Estimated\",\r\n                    indicator=\"orange\"\r\n                )\r\n\r\n            else:\r\n\r\n                frappe.msgprint(\r\n                    \"Return row for item \"\r\n                    + str(row.item_code)\r\n                    + \" has no traceable original receipt line and \"\r\n                    \"no per-unit Trade Offer to estimate from - \"\r\n                    \"Trade Offer Total has been set to 0. Enter it \"\r\n                    \"manually if this return should carry one.\",\r\n                    title=\"Return Trade Offer Skipped\",\r\n                    indicator=\"orange\"\r\n                )\r\n\r\n                trade_offer_total = 0.0\r\n\r\n        # ----------------------------------------------------\r\n        # QUANTITY BASIS\r\n        # ----------------------------------------------------\r\n\r\n        paid_qty = qty - scheme_qty\r\n\r\n        # ----------------------------------------------------\r\n        # COMMERCIAL VALUES\r\n        # ----------------------------------------------------\r\n\r\n        gross_total = vendor_rate * qty\r\n\r\n        discount_amount = (\r\n            vendor_rate\r\n            * discount_per\r\n            / 100.0\r\n            * paid_qty\r\n        )\r\n\r\n        trade_offer_per_unit = (\r\n            trade_offer_total / paid_qty\r\n            if paid_qty\r\n            else 0\r\n        )\r\n\r\n        # ----------------------------------------------------\r\n        # FORMULA NAMESPACE\r\n        # ----------------------------------------------------\r\n\r\n        namespace = {\r\n            \"rate\": vendor_rate,\r\n            \"qty\": qty,\r\n            \"custom_scheme_qty\": scheme_qty,\r\n            \"custom_discount_per\": discount_per,\r\n            \"custom_trade_offer\": trade_offer_per_unit,\r\n            \"custom_mrp\": mrp,\r\n            \"custom_gst_per\": gst_per,\r\n            \"custom_advance_tax_per\": advance_tax_per,\r\n            \"custom_fed_per\": fed_per,\r\n            \"custom_gross_total\": gross_total,\r\n            \"custom_discount_amnt\": discount_amount,\r\n            \"custom_trade_offer_total\": trade_offer_total,\r\n            \"custom_gst_amount\": 0.0,\r\n            \"custom_advance_tax_amount\": 0.0,\r\n            \"custom_fed_amount\": 0.0\r\n        }\r\n\r\n        # ----------------------------------------------------\r\n        # FORMULA NAMES\r\n        # ----------------------------------------------------\r\n\r\n        gst_formula_name = (\r\n            row.custom_gst_formula or \"\"\r\n        )\r\n\r\n        advance_tax_formula_name = (\r\n            row.custom_advance_tax_formula or \"\"\r\n        )\r\n\r\n        fed_formula_name = (\r\n            row.custom_fed_formula or \"\"\r\n        )\r\n\r\n        # ----------------------------------------------------\r\n        # THIRD SCHEDULE GST FORMULA OVERRIDE\r\n        # ----------------------------------------------------\r\n\r\n        if is_third_schedule:\r\n\r\n            current_gst_formula = (\r\n                gst_formula_name.lower()\r\n                if gst_formula_name\r\n                else \"\"\r\n            )\r\n\r\n            if not current_gst_formula.startswith(\"3rd\"):\r\n\r\n                matching_formula = frappe.db.get_value(\r\n                    \"Tax Formula\",\r\n                    {\r\n                        \"formula_name\": [\r\n                            \"like\",\r\n                            \"%3rd Schedule%\"\r\n                        ],\r\n                        \"formula_type\": \"gst\"\r\n                    },\r\n                    \"name\"\r\n                )\r\n\r\n                if matching_formula:\r\n\r\n                    gst_formula_name = matching_formula\r\n                    row.custom_gst_formula = matching_formula\r\n\r\n        # ----------------------------------------------------\r\n        # CALCULATE FED FIRST\r\n        #\r\n        # FED is included in inventory cost.\r\n        # ----------------------------------------------------\r\n\r\n        if fed_formula_name:\r\n\r\n            fed_amount = evaluate_formula(\r\n                fed_formula_name,\r\n                namespace\r\n            )\r\n\r\n        elif fed_per:\r\n\r\n            fed_amount = (\r\n                vendor_rate\r\n                * paid_qty\r\n                * fed_per\r\n                / 100.0\r\n            )\r\n\r\n        else:\r\n\r\n            fed_amount = 0.0\r\n\r\n        fed_amount = round(fed_amount, 4)\r\n\r\n        namespace[\"custom_fed_amount\"] = fed_amount\r\n\r\n        # ----------------------------------------------------\r\n        # CALCULATE GST PREVIEW\r\n        #\r\n        # GST is displayed only on Purchase Receipt.\r\n        # It is not added to PR Taxes and Charges.\r\n        # ----------------------------------------------------\r\n\r\n        if gst_formula_name:\r\n\r\n            gst_amount = evaluate_formula(\r\n                gst_formula_name,\r\n                namespace\r\n            )\r\n\r\n        else:\r\n\r\n            gst_amount = 0.0\r\n\r\n        gst_amount = round(gst_amount, 4)\r\n\r\n        namespace[\"custom_gst_amount\"] = gst_amount\r\n\r\n        # ----------------------------------------------------\r\n        # CALCULATE ADVANCE TAX PREVIEW\r\n        #\r\n        # Advance Tax is displayed only on Purchase Receipt.\r\n        # It is not added to PR Taxes and Charges.\r\n        # ----------------------------------------------------\r\n\r\n        if advance_tax_formula_name:\r\n\r\n            advance_tax_amount = evaluate_formula(\r\n                advance_tax_formula_name,\r\n                namespace\r\n            )\r\n\r\n        else:\r\n\r\n            advance_tax_amount = 0.0\r\n\r\n        advance_tax_amount = round(\r\n            advance_tax_amount,\r\n            4\r\n        )\r\n\r\n        namespace[\r\n            \"custom_advance_tax_amount\"\r\n        ] = advance_tax_amount\r\n\r\n        # ----------------------------------------------------\r\n        # INVENTORY COST\r\n        #\r\n        # Gross\r\n        # - Discount\r\n        # - Trade Offer\r\n        # + FED\r\n        # ----------------------------------------------------\r\n\r\n        inventory_amount = (\r\n            gross_total\r\n            - discount_amount\r\n            - trade_offer_total\r\n            + fed_amount\r\n        )\r\n\r\n        inventory_amount = round(\r\n            inventory_amount,\r\n            4\r\n        )\r\n\r\n        if qty:\r\n\r\n            net_unit_cost = (\r\n                inventory_amount / qty\r\n            )\r\n\r\n        else:\r\n\r\n            net_unit_cost = 0.0\r\n\r\n        net_unit_cost = round(\r\n            net_unit_cost,\r\n            4\r\n        )\r\n\r\n        # Preview including invoice-level taxes\r\n        line_total = (\r\n            inventory_amount\r\n            + gst_amount\r\n            + advance_tax_amount\r\n        )\r\n\r\n        line_total = round(\r\n            line_total,\r\n            4\r\n        )\r\n\r\n        price_after_taxes = round(\r\n            line_total / qty if qty else 0,\r\n            4\r\n        )\r\n\r\n    # --------------------------------------------------------\r\n    # CUSTOM DISPLAY FIELDS\r\n    # --------------------------------------------------------\r\n\r\n    row.custom_vendor_rate = round(\r\n        vendor_rate,\r\n        4\r\n    )\r\n\r\n    row.custom_scheme_qty = round(\r\n        scheme_qty,\r\n        4\r\n    )\r\n\r\n    row.custom_gross_total = round(\r\n        gross_total,\r\n        4\r\n    )\r\n\r\n    row.custom_discount_amnt = round(\r\n        discount_amount,\r\n        4\r\n    )\r\n\r\n    row.custom_trade_offer_total = round(\r\n        trade_offer_total,\r\n        4\r\n    )\r\n\r\n    row.custom_trade_offer = round(\r\n        trade_offer_per_unit,\r\n        4\r\n    )\r\n\r\n    row.custom_fed_amount = fed_amount\r\n\r\n    row.custom_gst_amount = gst_amount\r\n\r\n    row.custom_advance_tax_amount = (\r\n        advance_tax_amount\r\n    )\r\n\r\n    row.custom_price_without_taxes = (\r\n        net_unit_cost\r\n    )\r\n\r\n    row.custom_price_after_taxes = price_after_taxes\r\n\r\n    row.custom_line_total = line_total\r\n\r\n    # --------------------------------------------------------\r\n    # STANDARD ERPNext ITEM FIELDS\r\n    #\r\n    # Vendor price remains in custom_vendor_rate.\r\n    # Standard rate becomes inventory unit cost.\r\n    #\r\n    # Set only rate-related inputs.\r\n    # Let ERPNext calculate amount/base/net fields.\r\n    # --------------------------------------------------------\r\n\r\n    row.price_list_rate = round(\r\n        vendor_rate,\r\n        4\r\n    )\r\n\r\n    row.base_price_list_rate = round(\r\n        vendor_rate * float(doc.conversion_rate or 1),\r\n        4\r\n    )\r\n\r\n    row.rate = net_unit_cost\r\n\r\n    # Avoid ERPNext applying its own discount again\r\n    row.discount_percentage = 0\r\n    row.discount_amount = 0\r\n    row.distributed_discount_amount = 0\r\n\r\n    # Do not manually assign:\r\n    #\r\n    # row.amount\r\n    # row.net_rate\r\n    # row.net_amount\r\n    # row.base_rate\r\n    # row.base_amount\r\n    # row.base_net_rate\r\n    # row.base_net_amount\r\n    # row.valuation_rate\r\n    # row.stock_uom_rate\r\n    #\r\n    # ERPNext calculates these from rate and quantity.\r\n\r\n    # --------------------------------------------------------\r\n    # ACCUMULATE CUSTOM TOTALS\r\n    # --------------------------------------------------------\r\n\r\n    total_gross += gross_total\r\n    total_discount += discount_amount\r\n    total_trade_offer += trade_offer_total\r\n    total_fed += fed_amount\r\n    total_gst += gst_amount\r\n    total_advance_tax += advance_tax_amount\r\n    total_inventory_cost += inventory_amount\r\n    total_line_value += line_total\r\n\r\n\r\n# ============================================================\r\n# ROUND CUSTOM TOTALS\r\n# ============================================================\r\n\r\ntotal_gross = round(\r\n    total_gross,\r\n    4\r\n)\r\n\r\ntotal_discount = round(\r\n    total_discount,\r\n    4\r\n)\r\n\r\ntotal_trade_offer = round(\r\n    total_trade_offer,\r\n    4\r\n)\r\n\r\ntotal_fed = round(\r\n    total_fed,\r\n    4\r\n)\r\n\r\ntotal_gst = round(\r\n    total_gst,\r\n    4\r\n)\r\n\r\ntotal_advance_tax = round(\r\n    total_advance_tax,\r\n    4\r\n)\r\n\r\ntotal_inventory_cost = round(\r\n    total_inventory_cost,\r\n    4\r\n)\r\n\r\ntotal_line_value = round(\r\n    total_line_value,\r\n    4\r\n)\r\n\r\n\r\n# ============================================================\r\n# CUSTOM PURCHASE RECEIPT SUMMARY\r\n# ============================================================\r\n\r\ndoc.custom_total_gross = total_gross\r\n\r\ndoc.custom_total_discount = total_discount\r\n\r\ndoc.custom_total_trade_offer = (\r\n    total_trade_offer\r\n)\r\n\r\ndoc.custom_total_fed = total_fed\r\n\r\ndoc.custom_total_gst = total_gst\r\n\r\ndoc.custom_total_advance_tax = (\r\n    total_advance_tax\r\n)\r\n\r\ndoc.custom_grand_line_total = (\r\n    total_line_value\r\n)\r\n\r\n\r\n# ============================================================\r\n# PURCHASE RECEIPT TAX HANDLING\r\n#\r\n# GST and Advance Tax are only previews here.\r\n# They will be posted on Purchase Invoice.\r\n#\r\n# Clear taxes only when you do not use any standard PR\r\n# valuation taxes or landed-cost taxes.\r\n#\r\n# Recommended: do not clear doc.taxes automatically.\r\n# ============================================================\r\n\r\n# DO NOT run:\r\n#\r\n# doc.set(\"taxes\", [])\r\n#\r\n# because the Purchase Receipt may legitimately contain\r\n# valuation taxes, freight, customs, or other landed costs.\r\n\r\n\r\n# ============================================================\r\n# DO NOT MANUALLY SET DOCUMENT TOTALS\r\n#\r\n# ERPNext calculates all of these from item rates:\r\n# ============================================================\r\n\r\n# Do not assign:\r\n#\r\n# doc.total\r\n# doc.net_total\r\n# doc.base_total\r\n# doc.base_net_total\r\n# doc.total_taxes_and_charges\r\n# doc.grand_total\r\n# doc.base_grand_total\r\n# doc.rounded_total\r\n# doc.outstanding_amount\r\n# doc.payment_schedule\r\n\r\n\r\n# ============================================================\r\n# OPTIONAL DEBUG\r\n# ============================================================\r\n\r\nif SHOW_DEBUG:\r\n\r\n    debug_rows = []\r\n\r\n    row_number = 0\r\n\r\n    for row in doc.items:\r\n\r\n        row_number += 1\r\n\r\n        debug_rows.append(\r\n            \"<b>Row \"\r\n            + str(row_number)\r\n            + \": \"\r\n            + str(row.item_code)\r\n            + \"</b><br>\"\r\n            + \"Vendor Rate: \"\r\n            + str(row.custom_vendor_rate)\r\n            + \"<br>\"\r\n            + \"Qty: \"\r\n            + str(row.qty)\r\n            + \"<br>\"\r\n            + \"Scheme Qty: \"\r\n            + str(row.custom_scheme_qty)\r\n            + \"<br>\"\r\n            + \"Gross: \"\r\n            + str(row.custom_gross_total)\r\n            + \"<br>\"\r\n            + \"Discount: \"\r\n            + str(row.custom_discount_amnt)\r\n            + \"<br>\"\r\n            + \"Trade Offer: \"\r\n            + str(row.custom_trade_offer_total)\r\n            + \"<br>\"\r\n            + \"FED: \"\r\n            + str(row.custom_fed_amount)\r\n            + \"<br>\"\r\n            + \"GST Preview: \"\r\n            + str(row.custom_gst_amount)\r\n            + \"<br>\"\r\n            + \"Advance Tax Preview: \"\r\n            + str(row.custom_advance_tax_amount)\r\n            + \"<br>\"\r\n            + \"Inventory Unit Cost: \"\r\n            + str(row.custom_price_without_taxes)\r\n            + \"<br>\"\r\n            + \"ERPNext Rate: \"\r\n            + str(row.rate)\r\n            + \"<br>\"\r\n        )\r\n\r\n    frappe.msgprint(\r\n        (\r\n            \"<b>Purchase Receipt Cost Debug</b><br><br>\"\r\n            + \"<br>\".join(debug_rows)\r\n            + \"<hr>\"\r\n            + \"<b>Total Gross:</b> \"\r\n            + str(total_gross)\r\n            + \"<br>\"\r\n            + \"<b>Total Discount:</b> \"\r\n            + str(total_discount)\r\n            + \"<br>\"\r\n            + \"<b>Total Trade Offer:</b> \"\r\n            + str(total_trade_offer)\r\n            + \"<br>\"\r\n            + \"<b>Total FED:</b> \"\r\n            + str(total_fed)\r\n            + \"<br>\"\r\n            + \"<b>Total Inventory Cost:</b> \"\r\n            + str(total_inventory_cost)\r\n            + \"<br>\"\r\n            + \"<b>GST Preview:</b> \"\r\n            + str(total_gst)\r\n            + \"<br>\"\r\n            + \"<b>Advance Tax Preview:</b> \"\r\n            + str(total_advance_tax)\r\n        ),\r\n        title=\"Purchase Receipt Debug\",\r\n        indicator=\"blue\"\r\n    )",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "After Submit",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-08 21:13:47.067514",
  "module": null,
  "name": "Update price",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Receipt",
  "script": "# ============================================================\n# PURCHASE RECEIPT — UPDATE BUYING ITEM PRICE\n# Event: After Submit\n#\n# Source price:\n# custom_vendor_rate\n# ============================================================\n\nprice_list = (\n    doc.buying_price_list\n    or frappe.db.get_single_value(\n        \"Buying Settings\",\n        \"buying_price_list\"\n    )\n    or \"Standard Buying\"\n)\n\ncurrency = (\n    doc.currency\n    or frappe.db.get_single_value(\"Global Defaults\", \"default_currency\")\n    or \"PKR\"\n)\n\ncreated_count = 0\nupdated_count = 0\nunchanged_count = 0\nskipped_count = 0\n\n\nfor item in doc.items:\n\n    if not item.item_code:\n        skipped_count = skipped_count + 1\n        continue\n\n    vendor_rate = float(\n        item.custom_vendor_rate or 0\n    )\n\n    if vendor_rate <= 0:\n        skipped_count = skipped_count + 1\n        continue\n\n    item_uom = (\n        item.uom\n        or item.stock_uom\n        or frappe.db.get_value(\n            \"Item\",\n            item.item_code,\n            \"stock_uom\"\n        )\n    )\n\n    filters = {\n        \"item_code\": item.item_code,\n        \"price_list\": price_list,\n        \"buying\": 1,\n        \"currency\": currency\n    }\n\n    if item_uom:\n        filters[\"uom\"] = item_uom\n\n    # Keep this only if you want different prices per supplier.\n    if doc.supplier:\n        filters[\"supplier\"] = doc.supplier\n\n    existing_item_price = frappe.db.get_value(\n        \"Item Price\",\n        filters,\n        [\n            \"name\",\n            \"price_list_rate\"\n        ],\n        as_dict=True\n    )\n\n    if existing_item_price:\n\n        old_rate = float(\n            existing_item_price.price_list_rate or 0\n        )\n\n        if round(old_rate, 4) != round(vendor_rate, 4):\n\n            frappe.db.set_value(\n                \"Item Price\",\n                existing_item_price.name,\n                {\n                    \"price_list_rate\": round(\n                        vendor_rate,\n                        4\n                    )\n                },\n                update_modified=True\n            )\n\n            updated_count = updated_count + 1\n\n        else:\n            unchanged_count = unchanged_count + 1\n\n    else:\n\n        item_price = frappe.get_doc({\n            \"doctype\": \"Item Price\",\n            \"item_code\": item.item_code,\n            \"price_list\": price_list,\n            \"buying\": 1,\n            \"selling\": 0,\n            \"supplier\": doc.supplier,\n            \"currency\": currency,\n            \"uom\": item_uom,\n            \"price_list_rate\": round(\n                vendor_rate,\n                4\n            )\n        })\n\n        item_price.insert(\n            ignore_permissions=True\n        )\n\n        created_count = created_count + 1\n\n\nfrappe.msgprint(\n    (\n        \"<b>Buying prices processed</b><br>\"\n        + \"Created: \"\n        + str(created_count)\n        + \"<br>\"\n        + \"Updated: \"\n        + str(updated_count)\n        + \"<br>\"\n        + \"Unchanged: \"\n        + str(unchanged_count)\n        + \"<br>\"\n        + \"Skipped: \"\n        + str(skipped_count)\n    ),\n    title=\"Buying Price Update\",\n    indicator=\"green\"\n)\n",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 1,
  "api_method": "update_selling_price",
  "cron_format": null,
  "disabled": 1,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Insert",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-08 21:13:47.138185",
  "module": null,
  "name": "Update selling price rate",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": null,
  "script": "docname = frappe.form_dict.get(\"purchase_receipt\")\n\ndoc = frappe.get_doc(\"Purchase Receipt\", docname)\n\nselling_price_list = frappe.db.get_single_value(\n    \"Selling Settings\",\n    \"selling_price_list\"\n) or \"Standard Selling\"\n\nfor item in doc.items:\n\n    if not item.item_code or not item.custom_shelf_price:\n        continue\n\n    existing_item_price = frappe.db.get_value(\n        \"Item Price\",\n        {\n            \"item_code\": item.item_code,\n            \"price_list\": selling_price_list,\n            \"selling\": 1\n        },\n        [\"name\", \"price_list_rate\"],\n        as_dict=True\n    )\n\n    if existing_item_price:\n\n        if existing_item_price.price_list_rate != item.custom_shelf_price:\n\n            ip_doc = frappe.get_doc(\n                \"Item Price\",\n                existing_item_price.name\n            )\n\n            ip_doc.price_list_rate = item.custom_shelf_price\n            ip_doc.save(ignore_permissions=True)\n\n    else:\n\n        ip_doc = frappe.get_doc({\n            \"doctype\": \"Item Price\",\n            \"item_code\": item.item_code,\n            \"price_list\": selling_price_list,\n            \"selling\": 1,\n            \"currency\": frappe.db.get_single_value(\"Global Defaults\", \"default_currency\"),\n            \"price_list_rate\": item.custom_shelf_price\n        })\n\n        ip_doc.insert(ignore_permissions=True)\n\nfrappe.response[\"message\"] = \"Selling prices updated successfully\"",
  "script_type": "API"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Validate",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-08 20:11:56.818498",
  "module": "Aimatic",
  "name": "PurchaseOrderCalculation",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Purchase Order",
  "script": "# ============================================================\r\n# SERVER SCRIPT — Purchase Order — DYNAMIC COST ENGINE\r\n#\r\n# DocType : Purchase Order\r\n# Event   : Before Validate\r\n# ERPNext : v16\r\n#\r\n# PURPOSE\r\n# ------------------------------------------------------------\r\n# 1. Preserve vendor price in custom_vendor_rate\r\n# 2. Calculate:\r\n#       Gross\r\n#       Discount\r\n#       Trade Offer\r\n#       FED\r\n#       GST preview\r\n#       Advance Tax preview\r\n# 3. Inventory Cost:\r\n#       Gross - Discount - Trade Offer + FED\r\n# 4. Set ERPNext item rate to net inventory cost per unit\r\n# 5. Do not create GST/Advance Tax accounting rows on PR\r\n# 6. Let ERPNext calculate document totals\r\n#\r\n# FORMULA OPERATORS SUPPORTED:\r\n#       +  -  *  /  ( )\r\n# ============================================================\r\n\r\n\r\n# ============================================================\r\n# SETTINGS\r\n# ============================================================\r\n\r\nSHOW_DEBUG = 0\r\n\r\n\r\n# ============================================================\r\n# FORMULA ENGINE\r\n#\r\n# Server Script sandbox does not allow:\r\n# - eval()\r\n# - frappe.safe_eval()\r\n# - frappe.utils.safe_exec()\r\n#\r\n# This parser supports:\r\n# - addition\r\n# - subtraction\r\n# - multiplication\r\n# - division\r\n# - parentheses\r\n# - positive/negative numeric values\r\n# ============================================================\r\n\r\ndef evaluate_formula(formula_name, namespace):\r\n\r\n    if not formula_name:\r\n        return 0.0\r\n\r\n    expr = \"\"\r\n\r\n    try:\r\n        formula_doc = frappe.get_doc(\r\n            \"Tax Formula\",\r\n            formula_name\r\n        )\r\n\r\n        expr = (formula_doc.formula_string or \"\").strip()\r\n\r\n        if not expr:\r\n            return 0.0\r\n\r\n        # ----------------------------------------------------\r\n        # Replace formula variables with numeric values\r\n        # Longest variable names first\r\n        # ----------------------------------------------------\r\n\r\n        variable_names = sorted(\r\n            namespace,\r\n            key=len,\r\n            reverse=True\r\n        )\r\n\r\n        for variable_name in variable_names:\r\n\r\n            try:\r\n                variable_value = float(\r\n                    namespace.get(variable_name) or 0\r\n                )\r\n            except:\r\n                variable_value = 0.0\r\n\r\n            expr = expr.replace(\r\n                variable_name,\r\n                str(variable_value)\r\n            )\r\n\r\n        expr = expr.replace(\" \", \"\")\r\n\r\n        if not expr:\r\n            return 0.0\r\n\r\n        # ----------------------------------------------------\r\n        # Allow only arithmetic characters after substitution\r\n        # ----------------------------------------------------\r\n\r\n        allowed_characters = \"0123456789.+-*/()\"\r\n\r\n        for character in expr:\r\n            if character not in allowed_characters:\r\n                frappe.throw(\r\n                    \"Unsupported character '\"\r\n                    + str(character)\r\n                    + \"' in formula \"\r\n                    + str(formula_name)\r\n                )\r\n\r\n        # ----------------------------------------------------\r\n        # Recursive arithmetic parser\r\n        # ----------------------------------------------------\r\n\r\n        position = [0]\r\n        expression_length = len(expr)\r\n\r\n        def current_character():\r\n\r\n            if position[0] >= expression_length:\r\n                return \"\"\r\n\r\n            return expr[position[0]]\r\n\r\n        def consume_character():\r\n\r\n            character = current_character()\r\n            position[0] = position[0] + 1\r\n            return character\r\n\r\n        def parse_number():\r\n\r\n            number_text = \"\"\r\n\r\n            # Unary plus/minus\r\n            if current_character() in [\"+\", \"-\"]:\r\n                number_text += consume_character()\r\n\r\n            decimal_count = 0\r\n\r\n            while position[0] < expression_length:\r\n\r\n                character = current_character()\r\n\r\n                if character == \".\":\r\n                    decimal_count = decimal_count + 1\r\n\r\n                    if decimal_count > 1:\r\n                        frappe.throw(\r\n                            \"Invalid number in formula \"\r\n                            + str(formula_name)\r\n                        )\r\n\r\n                    number_text += consume_character()\r\n\r\n                elif character >= \"0\" and character <= \"9\":\r\n                    number_text += consume_character()\r\n\r\n                else:\r\n                    break\r\n\r\n            if (\r\n                not number_text\r\n                or number_text == \"+\"\r\n                or number_text == \"-\"\r\n                or number_text == \".\"\r\n                or number_text == \"+.\"\r\n                or number_text == \"-.\"\r\n            ):\r\n                frappe.throw(\r\n                    \"Invalid number near position \"\r\n                    + str(position[0])\r\n                    + \" in formula \"\r\n                    + str(formula_name)\r\n                )\r\n\r\n            return float(number_text)\r\n\r\n        def parse_factor():\r\n\r\n            character = current_character()\r\n\r\n            # Handle unary operators before parentheses/numbers\r\n            if character == \"+\":\r\n\r\n                consume_character()\r\n                return parse_factor()\r\n\r\n            if character == \"-\":\r\n\r\n                consume_character()\r\n                return -parse_factor()\r\n\r\n            if character == \"(\":\r\n\r\n                consume_character()\r\n\r\n                value = parse_expression()\r\n\r\n                if current_character() != \")\":\r\n                    frappe.throw(\r\n                        \"Missing closing parenthesis in formula \"\r\n                        + str(formula_name)\r\n                    )\r\n\r\n                consume_character()\r\n\r\n                return value\r\n\r\n            return parse_number()\r\n\r\n        def parse_term():\r\n\r\n            value = parse_factor()\r\n\r\n            while current_character() in [\"*\", \"/\"]:\r\n\r\n                operator = consume_character()\r\n                right_value = parse_factor()\r\n\r\n                if operator == \"*\":\r\n                    value = value * right_value\r\n\r\n                else:\r\n                    if right_value == 0:\r\n                        frappe.throw(\r\n                            \"Division by zero in formula \"\r\n                            + str(formula_name)\r\n                        )\r\n\r\n                    value = value / right_value\r\n\r\n            return value\r\n\r\n        def parse_expression():\r\n\r\n            value = parse_term()\r\n\r\n            while current_character() in [\"+\", \"-\"]:\r\n\r\n                operator = consume_character()\r\n                right_value = parse_term()\r\n\r\n                if operator == \"+\":\r\n                    value = value + right_value\r\n                else:\r\n                    value = value - right_value\r\n\r\n            return value\r\n\r\n        result = parse_expression()\r\n\r\n        if position[0] != expression_length:\r\n            frappe.throw(\r\n                \"Unexpected character near position \"\r\n                + str(position[0])\r\n                + \" in formula \"\r\n                + str(formula_name)\r\n            )\r\n\r\n        return round(float(result or 0), 4)\r\n\r\n    except Exception as error:\r\n\r\n        frappe.msgprint(\r\n            (\r\n                \"<b>Purchase Order Formula Error</b><br><br>\"\r\n                \"<b>Formula:</b> \"\r\n                + str(formula_name)\r\n                + \"<br>\"\r\n                \"<b>Expression:</b> \"\r\n                + str(expr)\r\n                + \"<br>\"\r\n                \"<b>Error:</b> \"\r\n                + str(error)\r\n            ),\r\n            title=\"Formula Error\",\r\n            indicator=\"red\"\r\n        )\r\n\r\n        return 0.0\r\n\r\n\r\n# ============================================================\r\n# TOTAL VARIABLES\r\n# ============================================================\r\n\r\ntotal_gross = 0.0\r\ntotal_discount = 0.0\r\ntotal_trade_offer = 0.0\r\ntotal_fed = 0.0\r\ntotal_gst = 0.0\r\ntotal_advance_tax = 0.0\r\ntotal_inventory_cost = 0.0\r\ntotal_line_value = 0.0\r\n\r\n\r\n# Prevent ERPNext Pricing Rules from applying another discount\r\ndoc.ignore_pricing_rule = 1\r\n\r\n\r\n# ============================================================\r\n# PROCESS Purchase Order ITEMS\r\n# ============================================================\r\n\r\nfor row in doc.items:\r\n\r\n    # --------------------------------------------------------\r\n    # READ INPUTS\r\n    # --------------------------------------------------------\r\n\r\n    vendor_rate = float(\r\n        row.custom_vendor_rate or 0\r\n    )\r\n\r\n    # Initial fallback only\r\n    if not vendor_rate:\r\n\r\n        vendor_rate = float(\r\n            row.price_list_rate\r\n            or row.rate\r\n            or 0\r\n        )\r\n\r\n    qty = float(row.qty or 0)\r\n\r\n    scheme_qty = float(\r\n        row.custom_scheme_qty or 0\r\n    )\r\n\r\n    discount_per = float(\r\n        row.custom_discount_per or 0\r\n    )\r\n\r\n    trade_offer_total = float(\r\n        row.custom_trade_offer_total or 0\r\n    )\r\n\r\n    mrp = float(\r\n        row.custom_mrp or 0\r\n    )\r\n\r\n    gst_per = float(\r\n        row.custom_gst_per or 0\r\n    )\r\n\r\n    advance_tax_per = float(\r\n        row.custom_advance_tax_per or 0\r\n    )\r\n\r\n    fed_per = float(\r\n        row.custom_fed_per or 0\r\n    )\r\n\r\n    is_third_schedule = int(\r\n        row.custom_is_3rd_schedule or 0\r\n    )\r\n\r\n    # --------------------------------------------------------\r\n    # VALIDATIONS\r\n    # --------------------------------------------------------\r\n\r\n    if qty < 0:\r\n        frappe.throw(\r\n            \"Quantity cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if scheme_qty < 0:\r\n        frappe.throw(\r\n            \"Scheme quantity cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if scheme_qty > qty:\r\n        frappe.throw(\r\n            \"Scheme quantity cannot exceed total quantity for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if discount_per < 0:\r\n        frappe.throw(\r\n            \"Discount percentage cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if discount_per > 100:\r\n        frappe.throw(\r\n            \"Discount percentage cannot exceed 100 for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    if vendor_rate < 0:\r\n        frappe.throw(\r\n            \"Vendor rate cannot be negative for item \"\r\n            + str(row.item_code)\r\n        )\r\n\r\n    # --------------------------------------------------------\r\n    # QUANTITY BASIS\r\n    # --------------------------------------------------------\r\n\r\n    paid_qty = qty - scheme_qty\r\n\r\n    # --------------------------------------------------------\r\n    # COMMERCIAL VALUES\r\n    # --------------------------------------------------------\r\n\r\n    gross_total = vendor_rate * qty\r\n\r\n    discount_amount = (\r\n        vendor_rate\r\n        * discount_per\r\n        / 100.0\r\n        * paid_qty\r\n    )\r\n\r\n    trade_offer_per_unit = (\r\n        trade_offer_total / paid_qty\r\n        if paid_qty\r\n        else 0\r\n    )\r\n\r\n    # --------------------------------------------------------\r\n    # FORMULA NAMESPACE\r\n    # --------------------------------------------------------\r\n\r\n    namespace = {\r\n        \"rate\": vendor_rate,\r\n        \"qty\": qty,\r\n        \"custom_scheme_qty\": scheme_qty,\r\n        \"custom_discount_per\": discount_per,\r\n        \"custom_trade_offer\": trade_offer_per_unit,\r\n        \"custom_mrp\": mrp,\r\n        \"custom_gst_per\": gst_per,\r\n        \"custom_advance_tax_per\": advance_tax_per,\r\n        \"custom_fed_per\": fed_per,\r\n        \"custom_gross_total\": gross_total,\r\n        \"custom_discount_amnt\": discount_amount,\r\n        \"custom_trade_offer_total\": trade_offer_total,\r\n        \"custom_gst_amount\": 0.0,\r\n        \"custom_advance_tax_amount\": 0.0,\r\n        \"custom_fed_amount\": 0.0\r\n    }\r\n\r\n    # --------------------------------------------------------\r\n    # FORMULA NAMES\r\n    # --------------------------------------------------------\r\n\r\n    gst_formula_name = (\r\n        row.custom_gst_formula or \"\"\r\n    )\r\n\r\n    advance_tax_formula_name = (\r\n        row.custom_advance_tax_formula or \"\"\r\n    )\r\n\r\n    fed_formula_name = (\r\n        row.custom_fed_formula or \"\"\r\n    )\r\n\r\n    # --------------------------------------------------------\r\n    # THIRD SCHEDULE GST FORMULA OVERRIDE\r\n    # --------------------------------------------------------\r\n\r\n    if is_third_schedule:\r\n\r\n        current_gst_formula = (\r\n            gst_formula_name.lower()\r\n            if gst_formula_name\r\n            else \"\"\r\n        )\r\n\r\n        if not current_gst_formula.startswith(\"3rd\"):\r\n\r\n            matching_formula = frappe.db.get_value(\r\n                \"Tax Formula\",\r\n                {\r\n                    \"formula_name\": [\r\n                        \"like\",\r\n                        \"%3rd Schedule%\"\r\n                    ],\r\n                    \"formula_type\": \"gst\"\r\n                },\r\n                \"name\"\r\n            )\r\n\r\n            if matching_formula:\r\n\r\n                gst_formula_name = matching_formula\r\n                row.custom_gst_formula = matching_formula\r\n\r\n    # --------------------------------------------------------\r\n    # CALCULATE FED FIRST\r\n    #\r\n    # FED is included in inventory cost.\r\n    # --------------------------------------------------------\r\n\r\n    if fed_formula_name:\r\n\r\n        fed_amount = evaluate_formula(\r\n            fed_formula_name,\r\n            namespace\r\n        )\r\n\r\n    elif fed_per:\r\n\r\n        fed_amount = (\r\n            vendor_rate\r\n            * paid_qty\r\n            * fed_per\r\n            / 100.0\r\n        )\r\n\r\n    else:\r\n\r\n        fed_amount = 0.0\r\n\r\n    fed_amount = round(fed_amount, 4)\r\n\r\n    namespace[\"custom_fed_amount\"] = fed_amount\r\n\r\n    # --------------------------------------------------------\r\n    # CALCULATE GST PREVIEW\r\n    #\r\n    # GST is displayed only on Purchase Order.\r\n    # It is not added to PR Taxes and Charges.\r\n    # --------------------------------------------------------\r\n\r\n    if gst_formula_name:\r\n\r\n        gst_amount = evaluate_formula(\r\n            gst_formula_name,\r\n            namespace\r\n        )\r\n\r\n    else:\r\n\r\n        gst_amount = 0.0\r\n\r\n    gst_amount = round(gst_amount, 4)\r\n\r\n    namespace[\"custom_gst_amount\"] = gst_amount\r\n\r\n    # --------------------------------------------------------\r\n    # CALCULATE ADVANCE TAX PREVIEW\r\n    #\r\n    # Advance Tax is displayed only on Purchase Order.\r\n    # It is not added to PR Taxes and Charges.\r\n    # --------------------------------------------------------\r\n\r\n    if advance_tax_formula_name:\r\n\r\n        advance_tax_amount = evaluate_formula(\r\n            advance_tax_formula_name,\r\n            namespace\r\n        )\r\n\r\n    else:\r\n\r\n        advance_tax_amount = 0.0\r\n\r\n    advance_tax_amount = round(\r\n        advance_tax_amount,\r\n        4\r\n    )\r\n\r\n    namespace[\r\n        \"custom_advance_tax_amount\"\r\n    ] = advance_tax_amount\r\n\r\n    # --------------------------------------------------------\r\n    # INVENTORY COST\r\n    #\r\n    # Gross\r\n    # - Discount\r\n    # - Trade Offer\r\n    # + FED\r\n    # --------------------------------------------------------\r\n\r\n    inventory_amount = (\r\n        gross_total\r\n        - discount_amount\r\n        - trade_offer_total\r\n        + fed_amount\r\n    )\r\n\r\n    inventory_amount = round(\r\n        inventory_amount,\r\n        4\r\n    )\r\n\r\n    if qty:\r\n\r\n        net_unit_cost = (\r\n            inventory_amount / qty\r\n        )\r\n\r\n    else:\r\n\r\n        net_unit_cost = 0.0\r\n\r\n    net_unit_cost = round(\r\n        net_unit_cost,\r\n        4\r\n    )\r\n\r\n    # Preview including invoice-level taxes\r\n    line_total = (\r\n        inventory_amount\r\n        + gst_amount\r\n        + advance_tax_amount\r\n    )\r\n\r\n    line_total = round(\r\n        line_total,\r\n        4\r\n    )\r\n\r\n    # --------------------------------------------------------\r\n    # CUSTOM DISPLAY FIELDS\r\n    # --------------------------------------------------------\r\n\r\n    row.custom_vendor_rate = round(\r\n        vendor_rate,\r\n        4\r\n    )\r\n\r\n    row.custom_gross_total = round(\r\n        gross_total,\r\n        4\r\n    )\r\n\r\n    row.custom_discount_amnt = round(\r\n        discount_amount,\r\n        4\r\n    )\r\n\r\n    row.custom_trade_offer_total = round(\r\n        trade_offer_total,\r\n        4\r\n    )\r\n\r\n    row.custom_trade_offer = round(\r\n        trade_offer_per_unit,\r\n        4\r\n    )\r\n\r\n    row.custom_fed_amount = fed_amount\r\n\r\n    row.custom_gst_amount = gst_amount\r\n\r\n    row.custom_advance_tax_amount = (\r\n        advance_tax_amount\r\n    )\r\n\r\n    row.custom_price_without_taxes = (\r\n        net_unit_cost\r\n    )\r\n\r\n    row.custom_price_after_taxes = round(\r\n        line_total / qty if qty else 0,\r\n        4\r\n    )\r\n\r\n    row.custom_line_total = line_total\r\n\r\n    # --------------------------------------------------------\r\n    # STANDARD ERPNext ITEM FIELDS\r\n    #\r\n    # Vendor price remains in custom_vendor_rate.\r\n    # Standard rate becomes inventory unit cost.\r\n    #\r\n    # Set only rate-related inputs.\r\n    # Let ERPNext calculate amount/base/net fields.\r\n    # --------------------------------------------------------\r\n\r\n    row.price_list_rate = round(\r\n        vendor_rate,\r\n        4\r\n    )\r\n\r\n    row.base_price_list_rate = round(\r\n        vendor_rate * float(doc.conversion_rate or 1),\r\n        4\r\n    )\r\n\r\n    row.rate = net_unit_cost\r\n\r\n    # Avoid ERPNext applying its own discount again\r\n    row.discount_percentage = 0\r\n    row.discount_amount = 0\r\n    row.distributed_discount_amount = 0\r\n\r\n    # Do not manually assign:\r\n    #\r\n    # row.amount\r\n    # row.net_rate\r\n    # row.net_amount\r\n    # row.base_rate\r\n    # row.base_amount\r\n    # row.base_net_rate\r\n    # row.base_net_amount\r\n    # row.valuation_rate\r\n    # row.stock_uom_rate\r\n    #\r\n    # ERPNext calculates these from rate and quantity.\r\n\r\n    # --------------------------------------------------------\r\n    # ACCUMULATE CUSTOM TOTALS\r\n    # --------------------------------------------------------\r\n\r\n    total_gross += gross_total\r\n    total_discount += discount_amount\r\n    total_trade_offer += trade_offer_total\r\n    total_fed += fed_amount\r\n    total_gst += gst_amount\r\n    total_advance_tax += advance_tax_amount\r\n    total_inventory_cost += inventory_amount\r\n    total_line_value += line_total\r\n\r\n\r\n# ============================================================\r\n# ROUND CUSTOM TOTALS\r\n# ============================================================\r\n\r\ntotal_gross = round(\r\n    total_gross,\r\n    4\r\n)\r\n\r\ntotal_discount = round(\r\n    total_discount,\r\n    4\r\n)\r\n\r\ntotal_trade_offer = round(\r\n    total_trade_offer,\r\n    4\r\n)\r\n\r\ntotal_fed = round(\r\n    total_fed,\r\n    4\r\n)\r\n\r\ntotal_gst = round(\r\n    total_gst,\r\n    4\r\n)\r\n\r\ntotal_advance_tax = round(\r\n    total_advance_tax,\r\n    4\r\n)\r\n\r\ntotal_inventory_cost = round(\r\n    total_inventory_cost,\r\n    4\r\n)\r\n\r\ntotal_line_value = round(\r\n    total_line_value,\r\n    4\r\n)\r\n\r\n\r\n# ============================================================\r\n# CUSTOM Purchase Order SUMMARY\r\n# ============================================================\r\n\r\ndoc.custom_total_gross = total_gross\r\n\r\ndoc.custom_total_discount = total_discount\r\n\r\ndoc.custom_total_trade_offer = (\r\n    total_trade_offer\r\n)\r\n\r\ndoc.custom_total_fed = total_fed\r\n\r\ndoc.custom_total_gst = total_gst\r\n\r\ndoc.custom_total_advance_tax = (\r\n    total_advance_tax\r\n)\r\n\r\ndoc.custom_grand_line_total = (\r\n    total_line_value\r\n)\r\n\r\n\r\n# ============================================================\r\n# Purchase Order TAX HANDLING\r\n#\r\n# GST and Advance Tax are only previews here.\r\n# They will be posted on Purchase Invoice.\r\n#\r\n# Clear taxes only when you do not use any standard PR\r\n# valuation taxes or landed-cost taxes.\r\n#\r\n# Recommended: do not clear doc.taxes automatically.\r\n# ============================================================\r\n\r\n# DO NOT run:\r\n#\r\n# doc.set(\"taxes\", [])\r\n#\r\n# because the Purchase Order may legitimately contain\r\n# valuation taxes, freight, customs, or other landed costs.\r\n\r\n\r\n# ============================================================\r\n# DO NOT MANUALLY SET DOCUMENT TOTALS\r\n#\r\n# ERPNext calculates all of these from item rates:\r\n# ============================================================\r\n\r\n# Do not assign:\r\n#\r\n# doc.total\r\n# doc.net_total\r\n# doc.base_total\r\n# doc.base_net_total\r\n# doc.total_taxes_and_charges\r\n# doc.grand_total\r\n# doc.base_grand_total\r\n# doc.rounded_total\r\n# doc.outstanding_amount\r\n# doc.payment_schedule\r\n\r\n\r\n# ============================================================\r\n# OPTIONAL DEBUG\r\n# ============================================================\r\n\r\nif SHOW_DEBUG:\r\n\r\n    debug_rows = []\r\n\r\n    row_number = 0\r\n\r\n    for row in doc.items:\r\n\r\n        row_number += 1\r\n\r\n        debug_rows.append(\r\n            \"<b>Row \"\r\n            + str(row_number)\r\n            + \": \"\r\n            + str(row.item_code)\r\n            + \"</b><br>\"\r\n            + \"Vendor Rate: \"\r\n            + str(row.custom_vendor_rate)\r\n            + \"<br>\"\r\n            + \"Qty: \"\r\n            + str(row.qty)\r\n            + \"<br>\"\r\n            + \"Scheme Qty: \"\r\n            + str(row.custom_scheme_qty)\r\n            + \"<br>\"\r\n            + \"Gross: \"\r\n            + str(row.custom_gross_total)\r\n            + \"<br>\"\r\n            + \"Discount: \"\r\n            + str(row.custom_discount_amnt)\r\n            + \"<br>\"\r\n            + \"Trade Offer: \"\r\n            + str(row.custom_trade_offer_total)\r\n            + \"<br>\"\r\n            + \"FED: \"\r\n            + str(row.custom_fed_amount)\r\n            + \"<br>\"\r\n            + \"GST Preview: \"\r\n            + str(row.custom_gst_amount)\r\n            + \"<br>\"\r\n            + \"Advance Tax Preview: \"\r\n            + str(row.custom_advance_tax_amount)\r\n            + \"<br>\"\r\n            + \"Inventory Unit Cost: \"\r\n            + str(row.custom_price_without_taxes)\r\n            + \"<br>\"\r\n            + \"ERPNext Rate: \"\r\n            + str(row.rate)\r\n            + \"<br>\"\r\n        )\r\n\r\n    frappe.msgprint(\r\n        (\r\n            \"<b>Purchase Order Cost Debug</b><br><br>\"\r\n            + \"<br>\".join(debug_rows)\r\n            + \"<hr>\"\r\n            + \"<b>Total Gross:</b> \"\r\n            + str(total_gross)\r\n            + \"<br>\"\r\n            + \"<b>Total Discount:</b> \"\r\n            + str(total_discount)\r\n            + \"<br>\"\r\n            + \"<b>Total Trade Offer:</b> \"\r\n            + str(total_trade_offer)\r\n            + \"<br>\"\r\n            + \"<b>Total FED:</b> \"\r\n            + str(total_fed)\r\n            + \"<br>\"\r\n            + \"<b>Total Inventory Cost:</b> \"\r\n            + str(total_inventory_cost)\r\n            + \"<br>\"\r\n            + \"<b>GST Preview:</b> \"\r\n            + str(total_gst)\r\n            + \"<br>\"\r\n            + \"<b>Advance Tax Preview:</b> \"\r\n            + str(total_advance_tax)\r\n        ),\r\n        title=\"Purchase Order Debug\",\r\n        indicator=\"blue\"\r\n    )",
  "script_type": "DocType Event"
 }
]